# 이 저장소에서 일하는 규칙

## 배포

**사용자가 「배포해 주세요」라고 하면 `docs/DEPLOY.md` 의 「매 배포」를 그대로
따른다.** 기억으로 하지 말고 그 문서를 열어서 한다 — 절차가 2026-09-10 에
바뀌었다.

요약하면 이렇다. 자세한 명령과 이유는 문서에 있다.

1. `master` 에 머지한다 → GitHub Actions 가 검사·빌드하고 꾸러미를
   **릴리스 자산**(`release-<sha12>`)으로 올린다
2. `gh release download` 로 받는다
3. `scp` 로 EC2 에 올린다
4. ops 꾸러미를 풀고 `deploy-release.sh` 를 `sudo` 로 돌린다
5. **확인까지 해야 끝이다** — 서비스 `active`, 로컬 200, `current` 가 새 릴리스,
   그리고 **이번 변경 문구가 빌드 안에 실제로 있는지** 확인

### 배포에서 하지 말 것

- **Windows 에서 빌드하지 않는다.** 꾸러미는 Linux 전용이다
- **EC2 에서 빌드하지 않는다.** 램이 911MB 라 돌고 있는 서비스가 죽는다
- **아티팩트 업로드 실패를 배포 실패로 읽지 않는다.** 본 배송지는 릴리스다
- **dev 서버가 떠 있는 워크트리에서 빌드하지 않는다.** `.next` 를 공유해
  사용자가 보고 있는 서버가 500 이 된다

### 사용자 것을 함부로 지우지 않는다

빌드 아티팩트·릴리스·서버 파일처럼 **사용자 계정에 속한 것을 지우기 전에
반드시 묻는다.** 2026-09-09 에 오래된 아티팩트 85개를 묻지 않고 지웠다.
되돌릴 수 없었고, 그렇게 하고도 문제는 안 풀렸다.

## 로컬 확인

`apps/web/.env.local` 은 **일부러 Supabase 를 비워 두었다.** 로컬은 파일
저장소(`LOCAL_STORE=1`)만 본다. 그래서 라이브러리·캐릭터 목록이 로컬에서는
비어 보인다 — 고장이 아니다. 운영 데이터가 필요한 흐름은 **배포 서버에서**
확인한다. 그 값을 채우면 로컬의 삭제 버튼이 운영 DB 의 진짜 데이터를 지운다.
