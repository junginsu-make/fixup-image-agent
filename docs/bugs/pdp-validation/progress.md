# 상세페이지 개선 실행 기록

기준 SHA: f82b21d61b96525be445c441f484609d06d0673e
브랜치: fix/pdp-integrated-improvement
원본 작업공간의 진행 중인 SNS·포스터·수동 SQL은 가져오거나 변경하지 않았다.

## W0
- 설계 §14의 전체 122개 추적행을 현행 기준에 등록했다. 아래 검증 계획은 설계 원문을 따른다.
- 기존 코드에 대한 단위/타입 검사 결과는 pdp-validation/w0-* 파일이다.
- 이전 보고서의 880개 통과 기록은 재사용하지 않는다.
- 리뷰 방식: 구현자가 별도 diff 검토를 수행한다. 독립 에이전트 리뷰로 표시하지 않는다.

## 이슈 추적 상태

| ID | 상태 | 기준 SHA | 재현/검증 계획 |
|---|---|---|---|
| A-1 | W1 구현·검증 완료 | f82b21d | W1 / T-SAVE |
| A-2 | W1 보호 완료, 후속 단계 남음 | f82b21d | W1→W2 / T-STATE, 되돌리기 |
| A-3 | W2 구현·검증 완료 | f82b21d | W2 / T-STATE |
| A-4 | W2 구현·검증 완료 | f82b21d | W2 / T-SECTION |
| A-5 | W2 구현·검증 완료 | f82b21d | W2 / T-MIGRATE, T-SECTION |
| A-6 | W2 구현·검증 완료 | f82b21d | W2 / T-STATE, 두 제품 교차 |
| A-7 | W2 구현·검증 완료 | f82b21d | W2 / T-STATE |
| A-8 | W1 구현·검증 완료 | f82b21d | W1→W2 / T-SAVE |
| A-9 | W1 구현·검증 완료 | f82b21d | W1 / T-SAVE, fake timers |
| A-10 | 미착수 | f82b21d | W3 / T-REF, 설정 복구 |
| A-11 | 미착수 | f82b21d | W2/W3 / T-STATE, T-REF |
| A-12 | W2 구현·검증 완료 | f82b21d | W2 / T-STATE |
| A-13 | W2 구현·검증 완료 | f82b21d | W2/W7 / 모드별 재기획·busy·오류 표시 |
| A-14 | 미착수 | f82b21d | W3 / 목록 오류와 현재 참조 표시, 제출 검증 |
| A-15 | W2 구현·검증 완료 | f82b21d | W2 / T-SECTION |
| A-16 | 미착수 | f82b21d | W2/W4 / 생성 중 reorder 대역 |
| A-17 | W2 구현·검증 완료 | f82b21d | W2 / T-STATE |
| A-18 | W2 구현·검증 완료 | f82b21d | W2 / T-MIGRATE |
| B-1 | W6 구현·검증 완료 (실제 브라우저) | (이 커밋) | W6 / T-EXPORT |
| B-2 | W6 구현·검증 완료 | (이 커밋) | W6 / 실제 width/height·원본 보존 |
| B-3 | W6 구현·검증 완료 | (이 커밋) | W6 / T-EXPORT |
| B-4 | W6 구현·검증 완료 | (이 커밋) | W6 / 폰트 없는 OS 검증 |
| B-5 | W6 구현·검증 완료 | (이 커밋) | W6 / T-EDITOR |
| B-6 | W6 구현·검증 완료 | (이 커밋) | W6 / T-EDITOR |
| B-7 | W1 보호 완료, 후속 단계 남음 | f82b21d | W1 보존→W2 undo / T-STATE |
| B-8 | W2 구현·검증 완료 | f82b21d | W2 / 선택 UUID 유지 |
| B-9 | W6 구현·검증 완료 (실제 브라우저) | (이 커밋) | W6 / 공통 완성본 렌더 |
| B-10 | 검증 후 제외 (기본 설정에서 그림자 없음) | (이 커밋) | W6 / T-EXPORT, 미재현이면 증거로 종결 |
| B-11 | W6 구현·검증 완료 | (이 커밋) | W6 / T-EDITOR |
| B-12-a | W6 구현·검증 완료 | (이 커밋) | W6 / 실제 다운로드 |
| B-12-b | W6 구현·검증 완료 | (이 커밋) | W6 / 오류 표시·원본 보존 |
| B-12-c | 미착수 | f82b21d | W6/W8 / 드래그 응답과 저장 수 |
| B-12-d | B-1 과 함께 해결 | (이 커밋) | W6 / T-EXPORT |
| B-12-e | 검증 후 제외 (화면·원본 비율 차 0.0000) | (이 커밋) | W6 / T-EXPORT |
| B-12-f | 검증 후 제외 (레이어에 pre-wrap 안 걸림) | (이 커밋) | W6 / 재현 시 수정 |
| B-12-g | W6 구현·검증 완료 (실제 결함이었다) | (이 커밋) | W2 / T-MIGRATE |
| C-1 | W1 구현·검증 완료 | f82b21d | W1 / T-INPUT, T-SETTLE; W4 recovery |
| C-2 | W1 구현·검증 완료 | f82b21d | W1/W4 / T-SETTLE |
| C-3 | W3 구현·검증 완료 | 97b892c | W3 / T-COST |
| C-4-a | W1 구현·검증 완료 (표 누락, 18:4x 코드로 확인) | f2bc96e | W3 / Fable 포함 성공·실패 meter |
| C-4-b | 미착수 | f82b21d | W3/W8 / 실제 호출별 meter |
| C-5 | W1 구현·검증 완료 | f82b21d | W1 / T-SETTLE |
| C-6 | W1 구현·검증 완료 | f82b21d | W1/W4 / 전역·개별 오류 분리 |
| C-7 | 미착수 | f82b21d | W8 / T-LIMIT, pagination |
| C-8 | W1 구현·검증 완료 | f82b21d | W1 / T-INPUT |
| C-9 | 미착수 | f82b21d | W1/W8 / T-LIMIT |
| C-10-a | W1 구현·검증 완료 | f82b21d | W1 / 스키마 먼저, 안정된 오류 |
| C-10-b | W1 구현·검증 완료 | f82b21d | W1/W3 / 상한+1 |
| C-10-c | 미착수 | f82b21d | W1/W8 / T-INPUT |
| D-1 | W3 구현·검증 완료 | d077dd0 | W3 / T-REF |
| D-2 | W3 구현·검증 완료 | d077dd0 | W3 / 동일 실행 의도 검사 |
| D-3 | 미착수 | f82b21d | W5 / T-PARSE |
| D-4 | 미착수 | f82b21d | W8 / T-BUNDLE |
| D-5 | 미착수 | f82b21d | W5/W8 / T-PARSE |
| D-6 | 미착수 | f82b21d | W5 / T-PARSE |
| D-7 | W3 구현·검증 완료 | 97b892c | W3/W5 / 잘림·길이 검증 |
| D-8 | 미착수 | f82b21d | W1/W3 / T-INPUT |
| D-9 | W3 구현·검증 완료 | d077dd0 | W3 / T-COST |
| D-10 | 미착수 | f82b21d | W8 / T-BUNDLE, 활성 prompt capture |
| D-11-a | W5 구현·검증 완료 | (이 커밋) | W5 / T-IDENTITY |
| D-11-b | W5 구현·검증 완료 | (이 커밋) | W5 / T-REVIEW |
| D-11-c | 미착수 | f82b21d | W8 / 호출 그래프 |
| D-11-d | 미착수 | f82b21d | W3/W8 |
| D-11-e | 미착수 | f82b21d | W3/W5/W8 |
| E-6-1 | W3 구현·검증 완료 | 97b892c | W3 / T-COST |
| E-6-2-a | 미착수 | f82b21d | W1/W4 / T-SQL |
| E-6-2-b | 미착수 | f82b21d | W4 / T-JOB |
| E-6-3-a | 미착수 | f82b21d | W1/W8 / T-SAVE, T-LIMIT |
| E-6-3-b | 미착수 | f82b21d | W2/W8 / 만료 경계 |
| E-6-3-c | W1 구현·검증 완료 (A-1 에 통합, 표 누락) | f2bc96e | W1 / T-SAVE |
| F-7-0 | 미착수 | f82b21d | W7/W8 / T-PLAN, T-LIMIT |
| F-7-1 | 미착수 | f82b21d | W3/W5/W8 |
| F-7-2 | W1 보호 완료, 후속 단계 남음 | f82b21d | W1/W4 / 두 라우트 T-SETTLE |
| F-7-3 | 미착수 | f82b21d | W5/W7 / 분석 실패 주입 |
| F-7-4 | W3 구현·검증 완료 | d077dd0 | W3 / 실행 endpoint·model capture |
| F-7-5 | W3 구현·검증 완료 | 97b892c | W3 / T-COST, 품질 payload |
| F-7-6 | W3 구현·검증 완료 | 97b892c | W3 / T-COST |
| F-7-7 | 미착수 | f82b21d | W3/W4 / 청크 독립 금액 |
| F-7-8 | W4 저장 완료, 화면 표시 남음 | 756fd02 | W4 / T-JOB |
| F-7-9 | 미착수 | f82b21d | W3/W8 / 성공·실패 meter와 limit |
| F-7-10-a | W3 구현·검증 완료 | d077dd0 | W3/W4 / T-COST |
| F-7-10-b | W1 보호 완료, 후속 단계 남음 | f82b21d | W1/W8 / T-INPUT, T-LIMIT |
| K-01 | W2 구현·검증 완료 | f82b21d | W2 / T-STATE |
| K-02 | W1 보호 완료, 후속 단계 남음 | f82b21d | W1/W2 |
| K-03 | W2 구현·검증 완료 | f82b21d | W2 / T-STATE |
| K-04 | W4 서버측 완료, 되찾기 UI 남음 | 7f2042f | W4 / T-JOB |
| K-05 | W1 보호 완료, 후속 단계 남음 | f82b21d | W1 보호/W4 완료 |
| K-06 | W1 보호 완료, 후속 단계 남음 | f82b21d | W1/W4 |
| K-07 | W1 구현·검증 완료 | f82b21d | W1/W4 |
| K-08 | 미착수 | f82b21d | W7 / T-PLAN |
| K-09 | W5 구현·검증 완료 | (이 커밋) | W5 / T-EVIDENCE |
| K-10 | W5 구현·검증 완료 | (이 커밋) | W5 / T-REVIEW |
| K-11 | W5 구현·검증 완료 | (이 커밋) | W5 / T-IDENTITY |
| K-12 | W5 구현·검증 완료 | (이 커밋) | W5 / T-REVIEW |
| K-13 | W1 구현·검증 완료 | f82b21d | W1/W4 |
| U-01 | W3 구현·검증 완료 | (코덱스 미커밋분) | W3 / T-REF |
| U-02 | 계획 완료, 배선 보류(사용자 결정) | (이 커밋) | W5 / T-IDENTITY |
| U-03 | W3 구현·검증 완료 | (이 커밋) | W2/W3 |
| U-04 | W3 구현·검증 완료 | (이 커밋) | W3 / T-REF |
| U-05 | 미착수 | f82b21d | W3 / 각도 선택 fixture |
| U-06 | 미착수 | f82b21d | W3/W7 / T-PLAN |
| U-07 | W3 구현·검증 완료 | d077dd0 | W3 / 모든 ratio payload |
| U-08 | 미착수 | f82b21d | W1/W7 |
| U-09 | W2 구현·검증 완료 | f82b21d | W2 / T-SECTION |
| U-10 | W5 구현·검증 완료 | (이 커밋) | W5/W7 |
| U-11 | W7 구현·검증 완료 | (이 커밋) | W2/W7 |
| U-12 | K-09 과 함께 해결(같은 검사·같은 정책) | (이 커밋) | W5/W7 |
| U-13 | W5 구현·검증 완료 | (이 커밋) | W5/W7 / T-EVIDENCE |
| U-14 | W7 구현·검증 완료 | (이 커밋) | W7 / T-PLAN |
| U-15 | W7 구현·검증 완료 | (이 커밋) | W2/W5/W7 |
| U-16 | 미착수 | f82b21d | W9 보고. 전환율 개선을 출시 합격으로 위장하지 않음 |
| U-17 | W3 구현·검증 완료 — 실호출로 확인 | 83af655 | W3 / T-MODEL |
| U-18 | 미착수 | f82b21d | W3/W5/W9 |
| X-01 | W2 구현·검증 완료 | f82b21d | T-SAVE/T-STATE |
| X-02 | W1 보호 완료, 후속 단계 남음 | f82b21d | T-SETTLE/T-COST |
| X-03 | 미착수 | f82b21d | T-MODEL/T-COST |
| X-04 | W1 보호 완료, 후속 단계 남음 | f82b21d | T-INPUT/T-JOB |
| X-05 | 미착수 | f82b21d | T-REF/T-PLAN |
| X-06 | 미착수 | f82b21d | 실제 config/output 대조 |
| X-07 | 미착수 | f82b21d | T-SECTION/T-EXPORT |
| X-08 | 미착수 | f82b21d | T-MODEL/T-REF |
| X-09 | 미착수 | f82b21d | W8 검증 산출물 |


## W1 — 손실·예약 응급 수정

- TDD RED: `w1-save-red.txt`(실제 저장 왕복 2건 실패), `w1-api-red.txt`(요청/정산 10건 실패), `w1-redesign-red.txt`(4건 실패), `w1-checkpoint-red.txt`, `w1-review-red.txt`(앞 섹션 선제출), `w1-partial-red.txt`.
- 새 저장시계/저장상태 모듈의 첫 RED는 모듈 부재다(`w1-autosave-red.txt`, `w1-library-red.txt`). 기존 동작의 실패 재현과 구분한다.
- GREEN: `w1-final-green.txt` — 관련 33파일 267개. `w1-typecheck.txt` — 웹 타입 검사. `w1-lint.txt` — 변경 제품 파일 오류 0, 기존 img 경고 4.
- 넓은 회귀: `w1-web-regression.txt` — 웹 2695 통과, 6 skip(부분 성공 중단 시험 추가 직전). 최종 부분 성공 중단 변경은 위 관련 267개에 포함된다.
- 화면 연결: `draft-ui.test.tsx`가 실제 PdpMakerClient를 React renderer로 구동하여 복원값의 편집기 전달과 30초 연속 입력 저장을 확인했다. 실제 브라우저 픽셀/E2E 확인과는 다르다.
- 코드 리뷰: 같은 구현자가 별도 diff 검토. 배치 옵션 조립 중 먼저 제출되는 결함을 RED로 재현해 전부 조립 후 제출하도록 수정. 이전 비동기 저장 응답이 새 작업 ID를 덮는 것을 저장시계 identity로 방지. IndexedDB abort는 추가 검증에서 기존 코드가 이미 reject함을 확인해 변경하지 않았다.
- 재분석/단건 재생성/편집기 섹션 삭제/텍스트 재기획·대표 이미지 교체 전 별도 보관 초안. 저장 실패하면 변경을 중단한다. 즉시 undo UI와 revision 모델은 W2에서 진행한다.
- 초안 필드 추가·텍스트 중간 상태 보존은 기존 저장소 안에서 적용했다. v3 migration/정본 상태는 W2다. 자동 저장의 전체 필드 dependency는 W2 문서 revision으로 교체한다.
- 리디자인 자동 저장은 로컬과 서버 저장 결과를 구분하고 실패를 지속 표시한다. 서버 영구 job 복구는 W4다.
- 정산 실패는 결과/원래 오류를 보존하지만 기존 만료 정책·late settlement/중복 정산의 DB 수준 검증은 W4에 남는다.
- 인증 후 bounded body 읽기 및 예약 전 구조 검증. MIME signature·픽셀/파일 상한 실측·rate-limit 정책 DB 변경은 W8이다. C-9는 입력 오류의 예약 방지만 해결했고 전체 분석 한도 정책은 미완이다.
- W1 회귀/리뷰를 통과한 뒤에만 W2를 착수한다. Fable·모델 단가·제품 보존 정책은 W3까지 변경하지 않았다.


## W2 — 작업 정본·단계·v3 초안

- TDD RED: `w2-state-red.txt`(구성안 왕복·새 장면), `w2-scenario-save-red.txt`(확정 전 자동 저장에 옛 편집본), `w2-metadata-red.txt`(공통 디자인 소실), `w2-review-red.txt`(동시 수정 사본·이미지 중복), `w2-retention-red.txt`, `w2-delete-red.txt`, `w2-scene-red.txt`.
- `w2-document-red.txt`, `w2-migration-red.txt`, `w2-revision-red.txt`는 새 계약/함수 부재 RED와 구분해 보관했다.
- `PdpEditor`는 부모 구성안의 sections를 controlled 값으로 사용한다. 저장과 재진입도 같은 섹션 배열을 사용하며 배치·옵션은 section ID로 따라간다.
- 신규 섹션·AI 응답에 안정된 UUID를 부여한다. 장면 수정은 이전 영문 장면과 섞지 않고 현재 장면을 사용한다. 기존 영어를 그대로 유지하라는 옛 시험은 새 설계 계약에 맞춰 RED→수정했다.
- 실제 IndexedDB v3 저장소(documents/revisions/assets), 낙관적 revision 검사, 충돌 사본 보관, copy-on-write 이관, 원본 v2 유지. 생성 이미지·대표 이미지·텍스트 참조를 revision JSON에 반복 저장하지 않고 assets에서 복구한다.
- `PDP_DOCUMENT_V3=1`에서 새 저장 경로. 기본값은 off이며 이미 v3로 저장한 작업은 off에서도 읽을 수 있다. 다른 프로젝트의 환경변수는 변경하지 않았다.
- 새 revision 저장은 이전 저장소의 원본을 삭제하지 않는다. quota/쓰기 실패, 다른 탭 충돌, 보관된 이미지·레이어 복구 시험 통과.
- 입력 필드 나열 대신 저장 snapshot의 변경값으로 revision을 관측한다. ID/알림 갱신은 dirty로 만들지 않는다.
- 데이터가 없는 텍스트 stage는 입력으로 복구한다. 01 버튼은 실제 텍스트 입력으로 돌아간다. 구성안에서 유료 결과 섹션을 삭제할 때도 보관하고 이전 초안으로 되돌릴 수 있다.
- 리뷰 보완: 확정 전 자동 저장도 편집기 사본과 구성안이 일치하도록 `editorForSections`를 공유했다. 원문 디자인 metadata를 정규화가 버리는 문제도 실제 저장 시험으로 고쳤다. 공개되지 않은 새로운 기능은 추가하지 않았다.
- GREEN: `w2-web-regression.txt` — 웹 2712 passed / 6 skipped. `w2-core.txt` — pdp-core 620 passed. `w2-typecheck.txt` 통과. `w2-lint.txt` 오류 0, 기존 img 경고 4.
- 실제 브라우저: `w2-browser.cjs` / `w2-browser.txt` — 분리 worktree의 Next dev(3107), Edge headless, 두 모드의 입력→수정/추가→생성→왕복→v3 저장/새로고침, 이미지 3장과 수정 문구 복구 통과. 유료 `/api/pdp` 호출은 전부 mock이며 실제 AI 품질 시험은 아니다.
- 코드 리뷰는 구현자 자체 diff 리뷰다. 새 문서의 서버 저장/승인 snapshot/QA 상태·원가 모델은 각각 W4/W5/W3에서 확장한다. v3의 보관기간 및 자산 GC·용량 측정은 E-6-3/W8 검증 과제이며 기존 v2 이관 전에 청소하지 않는다.
- 레거시에 원래 기록되지 않았던 원문·모델 선택은 복원할 수 없다. 새 필드 기본값과 보존된 결과를 사용한다.


## W3 — 기획 모델·참조·원가 (이어받아 진행)

코덱스가 실호출 검증에서 멈춘 지점을 이어받았다. 멈춘 원인부터 밝히고 고쳤다.

### 멈춘 원인 — 텍스트 기획이 잘려 죽었다

`w3-live-planning.txt` 의 두 줄이 그대로 증거다.

```
image : passed  (섹션 6개, 4호출, $1.00, 241초)
text  : failed  (INVALID_REQUEST, 111초)
```

추적 결과 `normalizeTextBlueprint`(`pdp.text-plan.ts:409`)의 「섹션이 0개」였다.
그 앞을 거슬러 가면 원인은 **모델이 답을 끝까지 못 썼는데 아무도 안 봤다**는 것이다.

- `viaAnthropic`(`apps/web/lib/pdp/providers.ts`)이 `stop_reason` 을 확인하지 않았다.
  `max_tokens` 로 끊긴 조각난 도구 인자가 정상 응답처럼 아래로 흘렀다.
- 코어가 `maxTokens: 8192` 를 손으로 실어 보내 제공자의 모델별 정책을 덮었다
  (`pdp.text-plan.ts` 의 `depsFrom`, `pdp.service.ts` 의 `legacyContentsClient`).
- 그래서 사용자는 111초를 기다리고 값을 다 치른 뒤 「구성안을 만들지 못했습니다」만 받았다.

이는 설계 §10.1 「잘림은 명시 오류다. 모델 종료 이유·응답 크기를 남긴다」와
Claude 리뷰 D-7 에 해당한다.

### 고친 것

| 항목 | 내용 | 증거 |
|---|---|---|
| D-7 | `stop_reason === "max_tokens"` 를 `PdpResponseTruncatedError` 로 즉시 던진다. 잘림은 예비 모델로 넘기지 않는다 — 같은 길이를 또 치르고 또 잘린다 | `providers.test.ts` 3건 RED→GREEN |
| D-7 | 기획 호출의 길이 상한을 목적별로 나눴다(`PLANNING_MAX_TOKENS = 32768`, 검수는 8192 유지) | 같은 시험 |
| D-7 | 잘림을 `AI_RESPONSE_INVALID` 로 매핑해 「처리 중 오류」로 뭉개지 않는다 | `pdp.planning-call.test.ts` |
| §3 | 이름→목적 표(`purposeOfCall`)를 **코어 한 곳**에 두고 제공자가 그것을 쓴다. 코어는 길이 상한을 손으로 정하지 않는다 | `pdp.planning-call.test.ts` 3건 |
| C-3 / E-6-1 | 대표 이미지가 `imageCreditUnits(model, 1)` 로 예약하고 **같은 값**으로 확정한다. flare 기준 1장 → 5장 | `route-reliability.test.ts` 2건 |
| F-7-6 | 섹션 수정의 예약과 차감이 같은 값에서 나온다(전에는 예약 4장·차감 1장) | `redesign/settlement.test.ts` |
| F-7-5 | 섹션 수정도 생성과 같은 길(fal · gpt-image-2.5 `max`)로 그린다. 키가 없을 때만 옛 직접 호출로 떨어진다 | 같은 시험 |

`finalize-safety.test.ts` 의 대표 이미지 행은 장수 리터럴 대신 **던지지 않는 갈래**를
쓰는지만 보도록 고쳤다. 장수는 `route-reliability.test.ts` 가 값으로 잰다 — 설계가 경계한
「소스 문자열 검사를 그대로 늘리는 방식」을 피한다.

### 이어받기 전에 이미 되어 있던 것 (코덱스 작업, 미커밋분에 포함)

- D-1: 참조 상한 초과를 `.slice()` 로 버리지 않고 `assertReferenceBudget` 이 거절한다
- D-2: `withModel` 이면 시스템 프롬프트도 「required」로 갈라진다
- U-01: 사용자 연출 지시가 정체성 보존을 이기지 못한다
- Fable 배선(`PDP_PLANNING_MODEL`), 기획 4xx 는 폴백으로 숨기지 않음, 실행 모델·폴백 사유 기록

### 검증

- `w3-continue-core-green.txt` — pdp-core 628 통과
- `w3-continue-web-green.txt` — 웹 2,723 통과 / 6 skip
- redesign-core 74 통과. 타입 검사 web·pdp-core·redesign-core 모두 0건
- **실호출은 다시 돌리지 않았다.** 텍스트 기획이 실제로 통과하는지는 W3 종료 전
  `w3-live-planning.cjs` 를 한 번 더 돌려 확인해야 한다(이미지 1회 $1 수준)

### 남은 W3 항목

D-9(단건·배치 모델 선택 규칙 통일), F-7-4(속도형 선택이 fal 고정 모델에 무시됨),
F-7-7(1장씩 분할로 올림 반복), F-7-9·F-7-10-a(전사·실패 경로 원가 기록),
C-4-a/b(기획·레퍼런스 분석 원가 기록), U-04~U-07, A-10·A-11·A-14, D-11-d/e.

D-1 은 **예약 전에 알리지 못한다.** `assertReferenceBudget` 이 제공자 안에서 던지므로
사용자는 예약을 잡은 뒤 오류를 본다(크레딧은 0장 확정으로 풀린다). 설계 §6.2 의
「예약/유료 제출 전에 알린다」를 채우려면 각도·조각 수를 예약 앞에서 세야 하는데,
캐릭터 조회가 `reservation.userId` 를 필요로 해 인증·예약 분리가 선행돼야 한다.
W4 의 job 계약에서 함께 처리하는 것이 맞다.

### W3 두 번째 묶음 — 모델 선택·원가·방향

| 항목 | 내용 | 증거 |
|---|---|---|
| F-7-4 | 화면의 선택(정밀형·속도형)이 **실제로 그리는 모델**을 정한다. 속도형→`nano-banana-pro`, 정밀형→`gpt-image-2.5-flare`. 전에는 무엇을 고르든 flare 가 그렸고 선택은 값에만 쓰였다 | `redesign/settlement.test.ts` 2건 |
| F-7-4 | 값도 실제로 그리는 모델에서 뽑는다. fal 키가 없어 옛 직접 호출로 떨어지면 그쪽 단가를 쓴다 | 같은 시험 |
| F-7-6 확장 | 리디자인 생성의 **예약과 차감이 같은 모델 단가**에서 나온다. 고치는 과정에서 예약만 실행 모델이고 차감은 옛 이름이던 불일치를 시험이 값으로 잡았다(예약 5 vs 차감 4) | `it.each(["google","openai"])` |
| F-7-10-a | 생성 실패 경로도 이미 나간 글값(`llmUsd`)을 장부에 남긴다. 분석은 끝났는데 첫 그림이 실패하면 그 요청이 0원으로 보였다 | 같은 파일 |
| U-07 | 화면비가 프롬프트의 `format.orientation` 을 정한다. 4:3·16:9 는 `horizontal`, 1:1 은 `square` | `pdp.orientation.test.ts` 6건 |
| U-07 배선 | **값만 고치면 아무 일도 안 일어난다.** 실제 생성 경로가 `aspectRatio` 를 안 넘기던 것을 시험으로 먼저 잡고(RED) 이었다 | 같은 파일의 생성 경로 시험 |
| D-9 | 이미지 모델은 **페이지가 정한다**로 통일했다. 단건만 `options.imageModel` 을 봤는데 조립기는 `page.imageModel` 로 덮어써, 값은 그 모델로 매기고 그림은 다른 모델로 그렸다 | `route-reliability.test.ts` 2건 |

C-4-a(텍스트 기획 원가)와 리디자인 분석의 `onUsage` 는 W1 에서 이미 이어져 있었다. 확인만 했다.

### 검증

- `w3-final-web-green.txt` — 웹 2,730 통과 / 6 skip
- `w3-final-core-green.txt` — pdp-core 635 통과. redesign-core 74 통과
- 타입 검사 web·pdp-core·redesign-core·shared 모두 0건

### W3 에서 의도적으로 넘긴 것과 이유

| 항목 | 왜 여기서 안 하나 | 어디로 |
|---|---|---|
| F-7-9 전사 계량·한도 | `reserve_generation` 이 네 가지 작업만 받는다(`202609070005_team_credit.sql:166`). 새 작업 종류는 **마이그레이션**이 필요하고, 그것을 앱보다 먼저 적용하면 운영이 멈춘다 | W4(DB 작업과 함께) |
| F-7-7 1장씩 분할 과금 | 화면(`redesign-wizard.tsx`)이 8장을 1장짜리 8번으로 쪼갠다. 논리 작업 단위 정산은 job 계약이 있어야 성립한다 | W4 |
| D-1 「예약 전에 알린다」 | 참조 장수를 세려면 캐릭터 조회가 필요한데 그것이 `reservation.userId` 를 요구한다. 인증·예약 분리가 선행돼야 한다 | W4 |
| U-04·U-05·U-06 | 인물/캐릭터 충돌 선택, 각도 정책 설명, 지시 목적 분리는 모두 **화면에 새 칸**이 필요하다 | W7 |
| A-11·A-14 | 레퍼런스 토글 상태와 캐릭터 목록 오류 표시. 화면 상태 작업이다 | W7 |
| C-4-b 레퍼런스 분석 원가 | `style-references` 는 예약 자체가 없다. 한도·용량과 함께 다뤄야 한다 | W8 |

### 실호출 재검증 — 통과 (2026-09-17 15:0x)

`w3-live-planning-pass.txt`. **두 모드 모두 Fable 로 통과했다.**

```
image : passed  섹션 6개 · anthropic/claude-fable-5 · $0.83 · 4호출 · 189초
text  : passed  섹션 6개 · anthropic/claude-fable-5 · $1.22 · 5호출 · 264초
```

- 텍스트 기획이 살아났다. 전(`w3-live-planning.txt`)에는 111초 뒤 `INVALID_REQUEST` 였다.
- 기획 호출은 전부 `purpose: planning` · `claude-fable-5` 다. 폴백은 한 번도 안 걸렸다.
- 출력 토큰이 이미지 13,395 · 텍스트 22,696 이다. **텍스트 기획이 옛 상한 8,192 를
  두 배 넘게 쓴다** — 잘린 이유가 이 숫자로 확인됐다. 지금 상한 32,768 은 그 위로 여유가 있다.
- 기획 한 번에 $0.8~1.2 다. 재작성이 붙으면 더 든다. W3 의 원가 기록이 이 값을 장부에 남긴다.

**돌리는 법**: `npx tsx docs/bugs/pdp-validation/w3-live-planning.cjs`.
`node` 로 직접 돌리면 안 된다 — 이 저장소의 `.ts` 는 매개변수 프로퍼티와 확장자 없는
import 를 쓰고, Node 의 타입 제거 모드는 둘 다 지원하지 않는다.

### 다음 차례가 먼저 할 일

W3 는 닫혔다. 다음은 W4(영구 생성·복구)이며, 그 안에서 F-7-9·F-7-7·D-1 의
「예약 전에 알린다」를 함께 처리한다.


## W4 — 영구 생성·복구 (진행 중)

설계 §8 은 테이블 넷·RLS·워커·정산 SQL 을 한 묶음으로 적어 두었지만, 그대로
한 번에 하면 검증이 얕아지고 **스키마는 되돌리기 어렵다.** 설계 §15 의
「독립 실패 시나리오 단위로 커밋」에 따라 넷으로 쪼갠다.

| 조각 | 내용 | 운영 영향 | 상태 |
|---|---|---|---|
| W4-a | 작업 상태 기계(순수 로직) | 없음 | **완료** |
| W4-b | 멱등·지문·lease 판단(순수 로직) | 없음 | **완료** |
| W4-c | SQL 마이그레이션 + repository | DB 적용 필요 | 미착수 |
| W4-d | job API + 워커 entrypoint | 배포 필요 | 미착수 |

### W4-a·W4-b — 무엇을 값으로 잴 수 있게 했나

`apps/web/lib/pdp/jobs/{state,claim}.ts`. **DB·네트워크를 모르는 순수 함수다.**

세 축이 서로 독립이라는 것이 핵심이다(설계 §8.2).

```
생성(generation) · 정산(settlement) · 보존(persistence)
```

- 정산이 실패해도 결과는 살아 있다. `result_available` 에서 `failed` 로 가는 길이 아예 없다
- 그림을 받았어도 저장 전에는 `done` 이라고 말하지 않는다
- 저장이 실패하면 **그 결과로** 다시 시도한다. 생성 축을 되돌리지 않는다 — 되돌리면 두 번 낸다
- 제출이 불확실하면(`submission=uncertain`) 자동 재전송하지 않는다. 공급자 멱등을
  확인하지 않았고, 확인 없이 보내면 두 번 낸다. 사람이 볼 일로 남긴다
- **생성이 끝났어도 정산·보존이 남으면 `isTerminal` 이 거짓이다.** 워커가 여기서
  손을 떼면 크레딧이 영영 안 닫힌다

멱등은 요청 key 만으로 판단하지 않고 **내용의 지문**을 함께 본다.
같은 key + 같은 지문이면 기존 작업을, 다른 지문이면 409 다. 끝난 작업도 같은
내용이면 그것을 돌려준다 — 다시 만들면 두 번 낸다. 섹션 **순서**는 지문에서 뺐다.
정렬을 바꾸는 날 같은 작업이 두 번 만들어지지 않게.

### 검증 — 변이로 시험을 시험했다

이 저장소는 「빨개질 수 없는 가드 시험」을 이미 겪었다(리뷰 기억 `fixup-vacuous-guard-tests`).
그래서 시험이 진짜 재는지 변이를 넣어 확인했다.

| 변이 | 잡혔나 |
|---|---|
| 결과 받은 뒤 실패로 가게 허용 | ○ |
| 불확실한 제출도 재전송 허용 | ○ |
| 저장 실패가 생성 축을 되돌림 | ○ |
| 정산이 남아도 완료로 판정 | **처음엔 ✗ → 시험 보강 후 ○** |
| 불확실 표시 제거 | ○ |
| 지문 불일치를 무시 | ○ |
| 섹션 순서를 지문에 포함 | ○ |
| revision 을 지문에서 뺌 | ○ |
| lease 를 항상 만료로 | ○ |
| 빈 lease 를 못 잡게 | ○ |

**4번이 처음에 안 잡혔다.** 시험이 `persisted`(비종료 상태)에서만 재고 있어서,
`completed` + 정산 미완 조합을 통과시켰다. 그 조합을 재는 시험을 더해 잡았다.
가장 중요한 경로였다 — 크레딧이 안 닫히는 자리다.

- `w4-state-green.txt` — jobs 33건 통과
- 웹 전체 2,763 통과 / 6 skip. 타입 검사 0건

### W4-c 에 넘기는 제약 (지금 확인한 것)

- `generation_events.operation` 은 **check 제약과 `reserve_generation` 화이트리스트
  두 곳**에 적혀 있다. 2026-09-08 에 한쪽만 고쳐 이미지 만들기·카드뉴스가 전부
  거절됐다(`202609140001` 머리말). 전사 계량(F-7-9)을 넣으려면 **둘을 함께** 고친다
- 적용 순서는 **SQL 먼저 → 코드 배포**다. 반대로 하면 예약이 거절되고 사용자는
  「사용량을 확인하지 못했습니다」만 본다
- 예약 만료는 10분이고 `reserve_generation` 이 부수적으로 청소한다. 워커가 더 오래
  도는 job 을 이 로직에 그대로 맡기면 안 된다(설계 §8.4)
- `requested_units` 상한은 `max_reserve_units()` 다(202607270001 에서 60 으로 올림)

### W4-c — 저장소 계약과 SQL (적용 안 함)

| 조각 | 상태 |
|---|---|
| W4-a 상태 기계 | 완료 |
| W4-b 멱등·지문·lease | 완료 |
| **W4-c 계약 + 로컬 구현 + SQL** | **완료 (DB 미적용)** |
| W4-d job API + 워커 | 미착수 |

**계약 → 로컬 구현 → SQL 순서로 했다.** 계약을 시험으로 못 박아야 SQL 이 그것을
만족하는지 볼 수 있고, SQL 을 먼저 쓰면 코드가 그 모양을 따라가게 된다.

- `jobs/repository.ts` — 저장 방식과 무관한 계약
- `jobs/local-repository.ts` — `LOCAL_STORE=1` 용 파일 구현. **메모리가 아니다.**
  이 기능이 하려는 일이 「다시 띄워도 되찾기」인데 메모리에 두면 로컬에서 확인할 수 없다
- `supabase/migrations/202609180001_pdp_jobs.sql` — 같은 계약의 운영 구현

저장소가 지키는 것 넷을 계약 시험(13건)이 잰다. 로컬 구현으로 돌지만 Supabase
구현도 같은 시험을 통과해야 한다.

1. 같은 요청을 두 번 받아도 한 번만 만든다. **열쇠는 사용자마다 따로다**
2. 남의 작업은 못 읽고 못 바꾼다. job ID 만 알아서는 있는지조차 모른다
3. 두 워커가 동시에 잡아도 하나만. 잡은 워커가 죽으면 시간이 지나 풀린다
4. 결과는 어떤 실패에도 안 사라진다. base64 는 저장소에 안 담고 경로만 남긴다

### SQL 이 하는 일과 안 하는 일

**기존 것을 한 글자도 안 건드린다.** 새 표 둘과 새 함수 둘만 더한다.
`generation_events` 의 제약도 `reserve_generation` 본문도 그대로다 — 그래서 이
SQL 을 먼저 적용해도 옛 코드가 그대로 돈다(아무도 이 값을 안 읽는다).

새 함수 둘은 설계 §8.4 가 요구한 것이다.

- `renew_pdp_job_lease()` — 예약은 10분 뒤 만료된다. 워커가 더 오래 도는 작업을
  그 로직에 맡기면 아직 만드는 중에 예약이 풀려 다음 요청이 `concurrent_limit` 에
  걸린다. **살아 있는 워커의, 그 작업의 예약만** 늘린다
- `settle_expired_pdp_job()` — 만료 뒤에 결과가 나오면 `finalize_generation` 이
  그 행을 안 건드려(`status`가 `reserved`가 아니다) **결과는 있는데 차감이 안 된다.**
  새 예약을 만들지 않고 그 행을 한 번만 성공으로 고쳐 적는다

`finalize_generation` 이 이미 중복 확정을 막고 있음을 확인했다(`if v_event.status =
'reserved'`). 설계 §8.4 의 「중복 확정은 추가 차감하지 않는다」는 이미 만족된다.

### 검증 — SQL 과 코드가 갈리지 않게 값으로 잰다

상태 이름이 두 곳에 있다(TS union · SQL check). 한쪽만 고치면 **그 상태로 가는
순간 DB 가 거절하고 이미 값을 치른 작업이 멈춘다.** 2026-09-08 에 `operation`
목록으로 같은 사고가 났다. 그래서 눈으로 대조하지 않고 시험이 잰다(16건).

변이 10종으로 시험을 시험했고 **두 번 헛시험을 잡았다.**

| 변이 | 결과 |
|---|---|
| 상태 하나 누락 / 멱등 열쇠 변형 / 회원 insert 허용 | ○ |
| 늦은정산 중복 허용 / 결과 없어도 정산 / RLS 끔 | ○ |
| **사용자 예약 전체 갱신** | **처음 ✗ → 보강 후 ○** |
| 남의 lease 갱신 허용 / 남의 예약까지 갱신 | ○ (보강으로 추가) |

**「사용자 예약 전체 갱신」이 처음에 안 잡혔다.** 시험이 파일 전체에서 문자열을
찾았는데 같은 낱말이 다른 함수에도 있어서, 정작 `renew` 함수에서 그 줄을 지워도
통과했다. 제 리뷰가 지적한 「자기대조 가드」와 같은 꼴이다. **함수 본문만 떼어내
재도록** 고쳤고, 그 과정에서 「남의 lease」·「남의 예약」 두 가지를 더 막았다.

- `w4c-green.txt` — jobs 62건 통과
- 웹 전체 2,792 통과 / 6 skip. 타입 검사 0건

### 적용 전에 알아야 할 것

- **이 SQL 은 아직 DB 에 적용하지 않았다.** 적용은 사용자 결정이다
- 순서: SQL 먼저 → 코드 배포. 코드가 먼저 가도 `PDP_JOBS_ENABLED` 가 꺼져 있으면
  안전하지만, 켜면 표가 없어 500 이 난다
- 되돌리기는 표 둘·함수 둘을 drop 하면 된다. 기존 표에 흔적이 없다
- 파일 안에 확인 쿼리를 적어 두었다(회원이 insert 를 못 하는지, 함수가 닫혀 있는지)

### W4-d(1/2) — 운영 저장소 구현

**SQL 은 2026-09-17 17:3x 에 사용자가 운영 DB 에 적용했다.** 그 표를 실제로 쓰는
코드를 얹는다. 아직 API·워커는 없고, 저장소 구현까지다.

- `jobs/supabase-repository.ts` — 운영 구현
- `jobs/index.ts` — 로컬·운영을 고르는 자리와 `PDP_JOBS_ENABLED` 스위치

계약 시험을 **두 구현이 나눠 쓰도록** 바꿨다(`__tests__/repository-contract.ts`).
로컬 파일 구현이 그 시험을 그대로 돈다.

#### 계약에서 `debugRaw` 를 뺐다 — 헛시험이 될 자리였다

「base64 를 안 담는다」를 계약에 두면, 운영 구현에서는 원문을 꺼낼 길이 없어
**빈 문자열을 보고 조용히 통과한다.** 지켜 주는 것이 없는 시험이다. 그래서
계약에서 빼고 **로컬 전용 시험**으로 옮겼다 — 거기서는 실제 파일 안을 들여다본다.

#### 운영 구현은 실제 DB 로 아직 안 돌렸다

이 저장소의 `.env.local` 은 Supabase 를 일부러 비워 두었고, 값을 채우면 **로컬
시험이 운영 데이터를 건드린다**(저장소 CLAUDE.md). 그래서 계약 전체가 아니라
**사고로 이어지는 지점**만 질의 모양으로 잰다(7건).

| 무엇이 사고인가 | 어떻게 막았나 |
|---|---|
| `user_id` 를 빠뜨리면 **남의 작업을 읽는다.** service role 은 RLS 를 지나친다 | 읽기·갱신·멱등 조회 셋 다 `user_id` 로 좁히는지 잰다 |
| 잡기를 조건 없이 쓰면 **두 워커가 같은 작업을 돌려** 두 번 낸다 | `updated_at` 조건부 update 인지 잰다 |
| `onConflict` 가 없으면 같은 섹션이 두 줄이 되어 **두 번 센다** | 충돌 열쇠 셋을 잰다 |

변이 5종(각 방어를 하나씩 제거)이 모두 잡혔다.
**실제 DB 검증은 첫 운영 실행 때 한다 — 그 전까지 이 구현은 미검증이다.**

#### 저장소 선택

`isLocalStoreEnabled` 하나에 맡긴다. 여기서 `process.env` 를 다시 읽으면 판정이
두 벌이 되고, 그중 하나만 고치는 날 **로컬이 운영 DB 를 건드린다.** 그 함수는
`NODE_ENV === "production"` 이면 무조건 거짓이라 운영에 로컬 저장소가 켜질 길이 없다.

`PDP_JOBS_ENABLED` 는 **기본 꺼짐**이고 `"1"` 일 때만 켜진다(`"true"` 도 안 켜진다).
표와 워커가 준비되기 전에 켜지면 만들기가 아예 막힌다.

- `w4d-green.txt` — 웹 2,804 통과 / 6 skip. jobs 74건. 타입 검사 0건

### 남은 것

| 조각 | 내용 |
|---|---|
| W4-d(2/2) | job API 세 개(`POST /jobs`, `GET /jobs/:id`, `POST /jobs/:id/stop`) |
| W4-e | 워커 entrypoint + systemd. **운영 실행기 설치 전에는 배포 완료가 아니다**(설계 §8.3) |

기존 생성 라우트를 job 경로로 바꾸는 것은 그다음이다. 신구 화면을 함께 지원하는
동안 **이중 제출하지 않는 것**이 조건이다(설계 §8.3).

### 방향 수정 — 워커를 만들지 않기로 했다 (2026-09-17 17:4x, 사용자 승인)

설계 §8 은 워커(별도 실행기 + systemd)를 전제로 쓰였다. 그런데 **워커 없이도
실질적인 문제는 해결된다.**

핵심은 이것이다 — **브라우저가 끊겨도 서버는 하던 일을 끝까지 한다.** 지금은 그
결과를 돌려줄 곳이 없어서 버려질 뿐이다. 결과를 표와 스토리지에 적어 두면
탭을 닫아도 되찾을 수 있다.

| 상황 | 워커 없이 | 워커까지 |
|---|---|---|
| 탭 닫았다 돌아옴 | 되찾음 | 되찾음 |
| 새로고침 | 되찾음 | 되찾음 |
| 컴퓨터가 꺼짐 | 되찾음 | 되찾음 |
| 한 묶음이 5분을 넘음 | 잘림(한 묶음은 5분 안에 끝난다) | 됨 |
| 그 순간 서버가 재시작 | 그 건 유실 | 이어서 함 |

**워커를 미루는 이유는 위험이다.** 운영 서버는 RAM 911MB 에 여유 445MB 이고
(`pdp.upload-budget.ts` 머리말), 거기에 새 상시 프로세스를 올리면 지금 도는
서비스가 죽을 수 있다. 얻는 것(뒤 두 줄)보다 잃을 수 있는 것이 크다.

표와 저장소 코드는 워커를 나중에 얹을 수 있게 만들어 두었다(`lease`·`claimNext`).

이에 따라 설계 §8.3 의 `POST /jobs` 와 `POST /jobs/:id/stop` 은 **지금 만들지
않는다.** 제출을 기존 생성 라우트가 하므로 주문 창구가 따로 필요 없고, 워커가
없으니 중단할 대상도 없다. 지금 만들면 죽은 코드다 — 워커를 들일 때 함께 만든다.

### 결과 보존 자리

`jobs/artifacts.ts`. **기존 `library` 버킷을 그대로 쓴다.**

그 버킷의 정책은 경로 첫 칸이 소유자다(`202607280001_server_library.sql`). 새
버킷을 만들면 정책을 한 벌 더 쓰고 **사용자가 SQL 을 또 적용해야 한다.**
라이브러리 목록은 `library_items` 표만 읽으므로 같은 버킷을 써도 섞이지 않는다
(스토리지를 훑는 코드가 없음을 확인했다).

경로: `{userId}/pdp-jobs/{jobId}/{sectionId}-{attempt}.{ext}`

- **`section_id` 는 AI 응답값이다.** `../` 가 들어오면 남의 자리에 쓴다 — 손질한다
- 시도 번호가 들어가야 **다시 만든 것이 앞 것을 덮지 않는다**
- 소유 판정은 칸 단위다. `startsWith("u1")` 로 재면 `u10` 이 통과한다

변이 5종(앞부분만 대조·거슬러오르기·이름 손질 생략·시도 번호 생략·작업 구분
생략)이 모두 잡혔다.

### 다음

1. 생성 라우트가 job 을 만들고 결과를 스토리지·표에 적기 ← **여기서 실제 효과가 난다**
2. 조회 API (`GET /api/pdp/jobs/:id`, 문서별 최근 작업)
3. 화면이 돌아왔을 때 찾아 보여주기

### 기록기 — 생성 경로에 붙일 안전한 껍데기

`jobs/recorder.ts`. **붙는 자리가 돈이 오가는 생성 경로다.** 여기서 던지면
사용자는 이미 값을 치른 그림을 못 받는다 — 원래 고치려던 것과 똑같은 손실이 된다.

규칙 둘을 시험이 잰다(10건).

1. **스위치가 꺼져 있으면 아무것도 안 한다.** 저장소를 만들지도 않는다
2. **어떤 실패도 밖으로 던지지 않는다.** 기록이 안 돼도 그림은 나간다

2번 때문에 「조용히 사라지는」 실패가 생기지 않도록, 못 적은 것은 상태에 남긴다
(`persistence: retry_required` + `errorCode`). 저장에 실패하면 **경로를 적지
않는다** — 적어 두면 없는 자리를 가리키고, 되찾을 때가 되어서야 드러난다.

#### 변이로 두 번 걸렀다

1차 7종 중 **둘이 안 잡혔다.** 둘 다 핵심 안전장치였다.

| 안 잡힌 변이 | 왜 못 잡았나 | 보강 |
|---|---|---|
| **기록 실패를 밖으로 던짐** | 「작업을 못 만들어도」 시험은 빈 기록기로 빠져 그 길을 안 지난다 | 작업은 만들어졌는데 그 뒤 DB 가 흔들리는 시험을 더했다 — 운영에서 가장 그럴듯한 모양이다 |
| **저장 실패해도 경로를 적음** | 상태만 보고 `outputPath` 를 안 봤다 | 경로가 비어 있고 실패 코드가 남는지 잰다 |

보강 뒤 8종이 모두 잡힌다.

구현 중 **업로드를 두 번 부르는 실수**를 스스로 발견해 고쳤다. `quietly` 로 한 번
감싸고 성공 여부를 알려고 또 한 번 불렀는데, 그러면 같은 그림을 두 번 올린다.

- 웹 2,823 통과 / 6 skip. 타입 검사 0건

### 다음 — 생성 라우트에 연결

이 껍데기를 `images/batch` 와 `images` 라우트에 붙인다. 스위치가 꺼져 있으면
지금과 한 글자도 다르지 않게 돈다.

### 생성 라우트 연결과 조회 API

`images/batch` 에 기록기를 붙이고, 되찾는 자리(`GET /api/pdp/jobs/:id`)를 만들었다.

**스위치가 꺼져 있으면 기록기를 만들지도 않는다.** 시험이 그것을 잰다 —
「꺼져 있으면 `createJobRecorder` 가 한 번도 안 불린다」와 「응답이 그대로다」.

붙인 자리는 셋이다.
- 제출 직전 `started()`
- **성공한 섹션마다** `sectionDone()` — 실패한 섹션은 적을 그림이 없다
- 마무리에 `finished()` — 성공 수와 정산 결과로 닫는다

`documentId` 는 화면이 아직 안 보낸다. 없으면 예약 식별자로 대신하므로 그때는
이 요청 한 건만 묶인다. 화면이 보내기 시작하면 문서 단위로 묶인다.

#### 조회는 돈이 안 든다

저장소를 읽고 그림 주소를 서명할 뿐이다. 남의 것이면 **404** 다 —
「권한 없음」으로 답하면 그 id 가 있다는 사실을 알려 주는 셈이 된다.

서명 주소는 한 시간짜리다. 버킷이 비공개라 주소만 알면 누구나 보는 상태가 되면
안 된다. **항목마다 실패할 수 있으므로** 못 만든 것만 빠진다 — 여덟 장 중 한
장이 잘못됐다고 일곱 장을 함께 잃으면 안 된다.

#### 변이가 또 둘을 걸렀다

| 안 잡힌 변이 | 왜 못 잡았나 | 보강 |
|---|---|---|
| **소유자를 하드코딩** | `auth` 도 저장소도 대역이라 누구로 찾는지 아무도 안 봤다 | 로그인한 id 로 찾는지 인자를 잰다 |
| **실패한 섹션도 적음** | 시험에 실패하는 섹션이 없었다 | 한 장 성공·한 장 실패를 섞어 잰다 |

- 웹 2,837 통과 / 6 skip. 타입 0건. 린트 0건

### 남은 것 — 화면 연결

서버는 준비됐다. 화면이 `documentId` 를 보내고, 돌아왔을 때 `GET /jobs/:id` 로
찾아 보여주는 일이 남았다. **그 전까지는 스위치를 켜도 사용자에게 보이는 변화가
없다** — 적히기만 하고 쓰이지 않는다.

### 화면 연결 — W4 의 마지막 조각

`job-recovery.ts`. **판단을 화면 밖에 둔다.** 「무엇을 덮고 무엇을 두는가」가
`.tsx` 안에 있으면 시험이 값으로 못 재고, 한 줄을 지워도 전부 통과한다
(`page-wire.ts` 머리말이 적은 그 구멍이다).

되찾기에서 가장 조심할 것은 **이미 있는 그림을 덮지 않는 것**이다. 되찾기가
사용자가 그 뒤에 한 작업을 지우면, 고치려던 손실을 다른 모양으로 다시 낸다.

건너뛰는 것 셋: 이미 그림이 있는 섹션 · 저장에 실패해 주소가 없는 섹션 ·
지금 구성안에 없는 섹션(그 사이 구성을 바꿨을 수 있다).

`documentId` 는 저장 전이면 **아무것도 안 싣는다.** 없는 id 를 지어내면 다음에
저장했을 때 두 개로 갈린다.

배선은 따로 잰다 — `jobRequestFields` 가 맞아도 화면이 그 값을 안 실으면 아무
일도 안 일어나고, 그 한 줄은 지워도 나머지 시험이 전부 통과한다. 변이 둘
(요청에서 뺌·부모가 안 넘김)이 잡히는 것을 확인했다.

- 웹 2,846 통과 / 6 skip. pdp-core 635 통과. 타입 0건

## W4 마무리 — 지금 상태

| 조각 | 상태 |
|---|---|
| W4-a 상태 기계 | 완료 |
| W4-b 멱등·지문·lease | 완료 |
| W4-c 계약·로컬 구현·SQL | 완료. **SQL 은 운영 DB 에 적용됨** |
| W4-d 운영 저장소·기록기·라우트·조회 API·화면 배선 | 완료 |
| W4-e 워커 | **안 만듦**(사용자 승인). 표와 코드는 나중에 얹을 수 있게 남겨 뒀다 |

### 아직 안 한 것과 그 이유

- **스위치를 켜 보지 않았다.** `PDP_JOBS_ENABLED` 는 꺼져 있고, 배포도 안 했다.
  운영 저장소 구현은 실제 DB 로 아직 안 돌렸다(§W4-d 참조)
- **되찾기 UI 가 없다.** `recoverableSections` 는 준비됐지만 화면이 아직 부르지
  않는다. 지금은 **적히기만 하고 쓰이지 않는다** — 켜도 사용자에게 보이는 변화가 없다
- 단건 라우트(`images/route.ts`)에는 기록기를 안 붙였다. 일괄이 주 경로다

### 다음 사람이 켜기 전에 할 일

1. 배포한다(스위치는 꺼진 채로). 이 상태에서 기존 동작이 그대로인지 본다
2. `PDP_JOBS_ENABLED=1` 로 켜고 **한 번 만들어 본다.** `pdp_generation_jobs` 에
   줄이 생기고 `library` 버킷에 `{userId}/pdp-jobs/...` 가 생기는지 확인한다
3. 그때가 운영 저장소 구현의 **첫 실제 검증**이다. 안 되면 스위치를 끄면 원상복구다
4. 되찾기 UI 는 그 뒤에 붙인다

## 추적표 갱신 (2026-09-17 18:2x)

**W3·W4 에서 고친 것이 표에 반영되지 않고 있었다.** 설계 §14 가 「코드 수정 완료
시 각 행에 커밋·시험 증거·상태를 추가한다」고 했는데 빠뜨렸다. 15건을 실제
상태로 고쳤다(89 → 74 미착수).

갱신한 항목: C-3, D-1, D-2, D-7, D-9, E-6-1, F-7-4, F-7-5, F-7-6, F-7-10-a,
U-01, U-07, U-17, K-04(서버측), F-7-8(저장측).

## 지금 남은 것 — 74건

배포가 남은 것이 아니라 **W5~W9 가 통째로 남았다.** 영역별로 이렇다.

| 영역 | 미착수 | 무엇인가 | 설계 단계 |
|---|---|---|---|
| **B 편집·내보내기** | 16 | 해상도 하락·레이어 좌표·완성본 저장·폰트 | W6 |
| **U 기획 UX** | 8 | 상품 종류/목적 분리·승인 카피·전체 페이지 확인 | W7 |
| **D-11 검수** | 5 | QA 미실행과 통과 구분·빈 심사 | W5 |
| **K 검수·기획** | 3 | 일부 심사만으로 통과·신뢰문구 불일치 | W5 |
| **C API** | 5 | 레퍼런스 용량·분석 한도·상태코드 | W8 |
| **F 리디자인** | 5 | 절단 고지·전사 계량·분할 과금 | W7/W8 |
| **E 저장** | 5 | 초안 용량 실측·만료 고지 | W8 |
| **X 시험·문서** | 6 | 시험 구멍·문서 정합성 | W8/W9 |

### 사용자가 실제로 겪는 것 중 안 고친 것 (B 계열)

전수 리뷰에서 **높음**으로 꼽았고 아직 그대로다.

- **B-2** 내보낸 그림이 원본의 40% 폭으로 떨어진다(1536 → 최대 920). 레이어를
  하나도 안 얹어도 모든 다운로드가 이 경로를 지난다
- **B-3** 「라이브러리에 저장」이 얹은 글자를 버린다. 다운로드·ZIP 과 다른 그림이 저장된다
- **B-4** 편집기 글꼴 두 개가 `@font-face` 로 선언된 적이 없다
- **B-1** 레이어 좌표가 화면 폭에 묶여 있다. 좁은 창에서 배치하면 잘린다

이 넷은 **서버를 안 건드린다.** 배포 위험이 W4 보다 낮고, 사용자가 받는
결과물에 바로 보인다.

### 배포에 대해

지금 배포하면 **달라지는 것이 없다**(스위치가 꺼져 있다). W4 를 실제로 켜 보려면
배포가 필요하지만, 되찾기 UI 가 없어 사용자에게 보이는 변화는 없다.

**B 계열을 먼저 고치고 함께 배포하는 편이 낫다** — 그쪽이 사용자가 바로 체감하고,
서버 구조를 안 건드려 되돌리기도 쉽다.

## W6 시작 — 편집·내보내기

사용자가 결과물에서 바로 겪는 것들이다. **서버를 안 건드려** 배포 위험이 W4 보다 낮다.

### B-4 — 고를 수 있는 글꼴이 실제로 없었다

편집기가 「Pretendard」와 「Noto Sans KR」을 보여 줬는데, 이 저장소가 `@font-face`
로 부르는 이름은 **`Pretendard Variable` 하나뿐**이고 Noto 는 파일도 선언도 없다.
고른 대로 안 찍히고 OS 기본 글꼴로 떨어진다 — 화면에도, 내려받은 그림에도.

**더 나빴던 것은 기본값이다.** `PdpEditor.tsx:1778`·`:1799` 가 새 레이어에
`'Pretendard'` 를 박아 넣고 있었다. 즉 **아무것도 안 고른 사용자의 모든 글자가**
그렇게 나갔다. 목록만 고쳤으면 이 자리는 그대로 남았을 것이다.

같은 사고가 이미 있었다. `layout.tsx` 머리말이 적어 두었다 — `packages/ui` 의
`--font-sans` 가 "Pretendard" 를 맨 앞에 적어 두고도 불러오지 않아 맑은 고딕으로
떨어졌다. **세 번째다.**

고친 것 넷:
- 목록을 실제로 부르는 이름(`Pretendard Variable`)으로 바꾸고, 없는 Noto 를 뺐다
- 없는 글꼴을 고를 수 없게 됐으니 기본값도 목록에서 뽑는다(`DEFAULT_FONT_FAMILY`)
- **이미 저장된 레이어를 읽을 때 되살린다**(`normalizeFontFamily`). 목록만 고치면
  고친 보람이 새 작업에만 돌아간다
- 남은 둘은 어느 기계에나 있는 것이라 부르지 않아도 된다(Georgia·고정폭).
  영문 전용임을 이름에 밝혔다 — 한글은 OS 기본으로 간다

시험이 `pretendard.css` 를 읽어 **실제 선언과 대조한다.** 눈으로 맞추면 다음에
또 갈린다. 변이 4종(없는 이름으로 되돌림·복구 안 함·기본값을 없는 글꼴로·복구가
아무것도 안 함)이 모두 잡힌다.

- 웹 2,858 통과 / 6 skip. 타입 0건

### B-2·B-3 — 내려받은 것과 저장된 것이 만든 것과 달랐다

두 가지가 같은 렌더 경로에서 나왔다.

**B-2 해상도.** 편집 캔버스는 `min(100%, 460px)` 이고 내보내기는 그 폭에 **2를
곱해** 구웠다. 1536px 로 만든 것이 최대 920px 로 나간다 — 폭 60%, 넓이 36%.
게다가 **레이어를 하나도 안 얹어도** 이 길을 지나 JPEG 로 바뀌었다.

레이어 좌표가 캔버스 폭 기준이라, **배율만 원본에 맞추면** 배치는 그대로 두고
해상도만 되찾는다. 원본 크기는 비율 표를 또 적지 않고 **그림에게 묻는다**
(`naturalWidth`) — 표를 두 벌 두면 갈린다.

- 얹은 것이 없으면 **다시 굽지 않고 원본 바이트를 그대로 준다**
- 확장자도 원래 형식을 따른다. png 를 `.jpg` 로 저장하면 여는 쪽이 헷갈린다
- 8배를 상한으로 둔다. 그 위는 브라우저 캔버스 한도에 걸려 **한 장도 못 받는다**

**B-3 라이브러리 저장.** 다운로드·ZIP 은 합쳐서 굽는데 라이브러리만 원본을
올려, 같은 작업인데 **내려받은 것과 저장된 것이 달랐다.** 같은 함수를 쓰게 했다.

**참고용 저장 둘은 그대로 뒀다.** 「참고 이미지로 저장」·「레퍼런스로 저장」은
다음 작업의 바탕이 되는 그림이라, 글자가 박힌 것을 쓰면 그 글자가 다음 그림에
따라 들어간다. 완성본 보관과 뜻이 다르다(리뷰 §14.2 의 판단). 시험이 그 구분을
지킨다.

변이 5종(배율 고정·항상 다시 구움·항상 jpg·화면이 고정배율·라이브러리가 원본만)이
모두 잡힌다. 배선은 따로 잰다 — 함수가 맞아도 화면이 안 부르면 아무 일도 안 일어난다.

- 웹 2,875 통과 / 6 skip. 타입 0건

### B-1 — 레이어 좌표가 화면 폭에 묶여 있었다

레이어 좌표가 **캔버스 CSS 픽셀 절대값**인데 캔버스는 `min(100%, 460px)` 라 창이
좁으면 함께 줄었다. 데스크톱에서 오른쪽에 붙여 놓은 글자가 휴대폰에서는 밖으로
나가 **잘린 채 구워졌다.**

**안쪽 폭을 460 으로 고정하고 겉만 줄인다.** 좌표의 뜻이 화면 폭과 무관해지므로
어느 기기에서 열어도 같은 자리다. 저장된 데이터를 건드리지 않는다 — 옛 초안도
그대로 열린다(설계 §5.1 의 「460px 기준으로 해석」과 같은 뜻이다).

- 끌 때도 배율을 반영한다(`react-rnd` 의 `scale`). 안 하면 잡은 자리와 실제가 어긋난다
- 줄인 만큼 **자리도 준다**. `transform` 은 보이는 크기만 줄이고 레이아웃 높이는
  그대로라, 안 하면 아래에 빈 공간이 남는다
- B-12-d(창 크기 바꾼 뒤 ZIP 이 옛 폭을 씀)도 함께 풀렸다. 폭이 늘 460 이다

#### 실제 브라우저가 두 결함을 잡았다

설계 §11 이 「실제 브라우저 이미지로 비교한다. 소스 문자열만으로 통과시키지
않는다」고 적은 자리다. 단위 시험 21건이 **전부 통과한 상태에서** 브라우저를
돌리니 둘이 나왔다.

| 결함 | 무엇이었나 |
|---|---|
| **격자 칸이 안 줄어듦** | 무대가 `display: grid` 라 칸의 기본 `min-width: auto` 가 걸렸다. 안쪽이 460px 고정이라 `min(100%, …)` 가 무시되고 좁은 화면에서도 460px 그대로였다 |
| **관찰자가 한 번도 안 붙음** | `useEffect` 의 딸림값이 갤러리↔편집 전환에 안 바뀌어 다시 돌지 않았다. 처음 돌 때 캔버스가 없었으므로 창을 줄여도 배율이 1 그대로였다 |

둘 다 **소스만 봐서는 맞아 보인다.** `ref` 콜백으로 바꾸고 `min-width: 0` 을 넣어
고쳤다.

- `w6-canvas-browser.txt` — 논리 폭 460 고정(겉은 460→282), 자리=보이는 높이, 되돌려도 그대로
- 웹 2,896 통과 / 6 skip. 타입 0건. 린트 오류 0(기존 img 경고만)

### B-9 — 갤러리와 이어보기가 얹은 글자를 안 보여 줬다

이어보기 옆에는 「실제 상세페이지처럼 위에서 아래로 이어 붙인 모습입니다」라고
적혀 있는데, 정작 **얹은 글자가 하나도 안 보였다.** 최종 모습을 확인하려고 여는
화면인데 확인이 안 됐다. 개수만 「레이어 3」 배지로 알려 줬다.

B-1 로 레이어 좌표가 460 기준으로 고정됐으므로, **보여 줄 폭에 맞춰 배율만
곱하면** 같은 자리에 그려진다. 다시 굽지 않으니 값도 시간도 안 든다.

- `SectionPreview.tsx` 하나를 격자·이어보기 **두 곳이 함께 쓴다.** 한 곳만
  고치면 화면마다 달라진다 — 시험이 두 곳 모두 쓰는지 센다
- **스타일은 편집기와 같은 함수**(`buildOverlayShellStyle` 등). 여기서 따로
  지으면 `pdp-canvas-utils` 머리말이 경고한 그대로가 된다
- 누르는 것을 가로채지 않는다(`pointer-events: none`). 썸네일은 눌러서 크게 본다

- `w6-preview-browser.txt` — 편집기에서 얹은 글자가 갤러리에도 보이는 것을
  실제 브라우저로 확인했다
- 웹 2,903 통과 / 6 skip. 타입 0건

### B-5·B-6·B-11 — 편집기에서 고른 대로 안 보이던 셋

**B-6 줄맞춤.** 가운데·오른쪽으로 바꾸면 상자를 함께 넓혀 주는데 **캔버스를
벗어나는지 안 봤다.** 42px 글자를 왼쪽(x=52)에 두고 가운데 정렬하면 폭 420 이
되어 오른쪽 끝이 472 — 460 짜리 캔버스에서 잘린다. 남은 자리만큼만 넓힌다.
이미 넓으면 줄이지 않는다(사용자가 정한 폭이다).

**B-11 새 레이어.** 늘 같은 자리(52,52)라 문구를 여러 개 얹으면 **정확히
포개졌다.** 사용자는 하나만 생긴 줄 알고 또 누른다. 빈자리를 찾아 대각선으로
비켜 놓되 캔버스 밖으로는 안 나간다.

**B-5 확대 모달.** 모달 안의 큰 그림에 `data-zoomable` 이 붙어 있어 전역 뷰어가
**한 겹 더 열렸다.** 두 keydown 이 함께 돌아 Esc 한 번에 둘 다 닫히고, 화살표를
누르면 뒤에 가려진 모달만 조용히 넘어갔다. 이 화면은 자기 뷰어를 이미 갖고 있다
(설계 §11: 「확대보기는 한 뷰어만 소유한다」).

**B-8 은 이미 고쳐져 있었다.** `index < current ? current - 1 : current` 가
들어가 있다. 표에만 안 적혔다 — 시험으로 고정해 두었다.

#### 변이가 또 둘을 걸렀다

| 안 잡힌 변이 | 왜 못 잡았나 | 보강 |
|---|---|---|
| **캔버스 밖으로 밀어냄** | 시험이 대각선을 40칸 다 채워서, 빈자리를 못 찾고 기본 자리로 돌아오는 갈래를 탔다 | 15칸만 채워 **실제로 빈 자리를 찾는지** 잰다 |
| **뷰어 겹침 되돌림** | 잘라낸 구간에 주석의 `data-zoomable` 이 들어가 늘 걸렸다 | **주석을 빼고** 코드만 본다 |

- 웹 2,915 통과 / 6 skip. 타입 0건

### B-12-a·B-12-b — 받다 말거나, 말없이 비어 있거나

**B-12-a 다운로드.** `<a>` 를 문서에 안 붙이고 클릭했고, 주소를 `0ms` 뒤에
거뒀다. 붙이지 않으면 일부 브라우저가 클릭을 무시하고, 0ms 는 너무 일러 큰
ZIP 이 **받기를 시작도 못 하고** 주소가 사라질 수 있다. 붙였다 치우고, 1분 뒤에
거둔다 — 안 거두면 그 파일이 메모리에 계속 남는다.

이 저장소의 다른 다운로드(`image-viewer.tsx`)는 이미 붙이고 있었다. 두 곳이
다르면 한쪽만 고쳐지는 날이 온다.

**B-12-b 그림 오류.** 편집 캔버스에도 미리보기에도 `onError` 가 한 건도 없었다.
손상된 초안을 열면 **빈 자리 위에 글자만** 떠 있고, 사용자는 무엇이 잘못됐는지
모른 채 이미지가 사라졌다고 본다. 둘 다 알린다.

#### 이 저장소의 시험은 DOM 없이 돈다

`vitest.config.ts` 에 jsdom 이 없다. 그래서 `document.body` 를 가로채는 시험은
못 쓴다 — 소스에서 재고, 브라우저 동작은 `w6-*-browser.cjs` 가 본다. 그 한계를
시험 안에 적어 두었다.

변이 4종 중 **하나가 안 잡혔다.** 「편집기 오류 표시 제거」를 파일 전체에서
`onError` 로 찾는 바람에 다른 자리의 것에 걸렸다. **그 `<img>` 안만** 보도록
고쳤다.

- 웹 2,919 통과 / 6 skip. 타입 0건

### B-12-g — 되살린 키가 그리는 섹션과 안 맞을 수 있었다

레이어와 섹션별 설정은 순서가 아니라 **고유 키**로 저장한다. 그런데 그 키를
되살릴 때 「초안의 키 개수 === **초안의** 섹션 개수」로 확인했다. **화면이
실제로 그리는 것은 `initialResult` 의 섹션**이므로, 둘이 다르면 짝이 어긋난 채
통과하고 레이어가 남의 섹션에 붙는다.

원문에는 「도달 경로 불명」이라 적혀 있었는데, **비교 대상 자체가 틀렸다.**
그리는 섹션과 비교하도록 고쳤다. 같은 식이 화면에 두 번 적혀 있어 함수로 뺐다 —
한 곳만 고치는 날이 온다.

변이 3종 중 하나(「화면이 초안 길이로 비교」)가 처음에 안 잡혔다. 호출 횟수만
세고 **무엇을 넘기는지** 안 봤다. 넘긴 값까지 재도록 고쳤다.

### B-10·B-12-e·B-12-f — 재현되지 않아 종결한다 (근거 있음)

설계 §11 이 「실제 브라우저 이미지로 비교한다. 라이브러리 지원표나 소스 문자열만
으로 통과시키지 않는다」고 한 자리다. `w6-verify-browser.txt` 로 실측했다.

| 항목 | 지적 | 실측 | 판단 |
|---|---|---|---|
| B-10 | html2canvas 가 그림자 blur 에 scale 을 안 곱한다 | 기본 설정의 글자 그림자 **없음**(`shadowEnabled: false`) | 기본값에서는 나타나지 않는다. 사용자가 켰을 때만 생기는 미세한 차이로, 고치려면 렌더러를 바꿔야 한다 — 값에 비해 위험이 크다 |
| B-12-e | `object-fit: cover` 가 무시돼 찌그러진다 | 화면 비율 0.750 = 원본 0.750, **차이 0.0000** | 높이를 원본 비율로 계산하므로 `cover` 가 무시돼도 결과가 같다 |
| B-12-f | `white-space: pre-wrap` 이 미해석된다 | 레이어 span 의 `whiteSpace` 가 **null**(그 스타일이 안 걸림) | 지적의 전제가 성립하지 않는다 |

**셋 다 「고칠 것 없음」이 아니라 「지금은 재현되지 않음」이다.** 그림자를 켜는
기능이 기본이 되거나 렌더러를 바꾸면 B-10 은 다시 봐야 한다.

- 웹 2,927 통과 / 6 skip. 타입 0건

## 독립 리뷰 대응 (2026-09-17 19:3x)

B 계열 6개 커밋을 독립 리뷰에 넘겨 **높음 1 · 중간 8 · 낮음 6** 을 받았다.
판정은 두 번 다 REQUEST CHANGES 였다. **지적이 전부 맞았고** 그중 둘은 B-9 가
만들려던 것을 정면으로 깨뜨리고 있었다.

### 고친 것

| 심각도 | 무엇이 잘못됐나 | 어떻게 고쳤나 |
|---|---|---|
| **높음** | 라이브러리 저장이 여덟 장을 **동시에** 다시 구웠다. html2canvas 는 호출마다 문서 전체를 iframe 으로 복제하고 1536×2752 캔버스를 만든다 — 휴대폰에서 탭이 죽는다. 게다가 **한 장 실패 = 여덟 장 전부 실패** | ZIP 과 같은 순차 루프로. 못 구운 장은 원본 바이트로 올리고 어느 섹션이 그랬는지 알린다 |
| **중간** | 갤러리 썸네일이 `aspect-[3/4]` + `object-cover` 로 그림을 **자르는데 글자는 안 잘린 좌표로** 그려졌다. 9:16·1:1 에서 글자가 딴 자리에 뜬다 — 사용자가 미리보기를 보고 **멀쩡한 배치를 옮기게 된다** | 썸네일에서 자르기를 없앴다. 빈 섹션만 3:4 자리를 잡는다 |
| **중간** | 편집기의 `z-index` 두 줄이 미리보기에 안 따라와, **배경이 글자를 덮었다** | 배경 0 · 글자 1 을 명시 |
| **중간** | `toStoredLayer`/`toCanvasLayer` 는 **제품 호출자 0** 인데 시험 21건 중 18건이 그것을 쟀다 | 두 함수와 시험을 지우고 「460 고정 좌표계다」를 머리말에 적었다 |
| **중간** | `460` 이 서로 모르는 네 곳에 있고 `canvasFitFor(460)===1` 같은 **자기대조**만 있었다 | CSS 를 읽어 대조하는 가드. CSS 만 520 으로 바꾸는 변이가 잡히는 것을 확인했다 |
| **중간** | `anchorWorkbenchToOverlay` 가 축소 배율을 몰라 좁은 화면에서 작업대가 엉뚱한 자리에 붙었다 | `canvasFit` 을 받아 환산 |
| **중간** | 같은 4~5MB data URL 을 **두 번 디코드**했다 | `buildExportNode` 가 크기를 함께 돌려준다 |
| **중간** | 「참고용 저장은 원본을 유지한다」 시험 두 건이 **빨개질 수 없었다**(`generatedImage` 는 화면 표시용으로 스물여섯 군데 있다) | 해당 블록만 잘라 `captureSectionBlob` 이 **없음**을 잰다 |
| **중간** | `nextLayerOrigin` 이 세로 한계에 **폭에서 온 값**을 썼다. 1:1 은 열일곱째쯤부터 새 레이어가 캔버스 아래로 나갔다 | 상자 크기와 캔버스 높이를 받는다(`canvasHeightFor`) |
| 낮음 | 상자 폭을 안 봐서 헤드라인(360)이 오른쪽으로 넘쳤다 | 같은 수정에 포함 |
| 낮음 | 죽은 import(`sanitizeSectionFileName`) | 제거 |

### 내가 또 같은 함정에 빠졌다

리뷰 대응 중 「한 장씩 굽는다」 시험이 **내가 쓴 주석의 `Promise.all` 때문에**
빨개졌다. 리뷰가 지적한 것과 같은 꼴이다 — 주석을 걷어내고 재도록 고쳤다.
이 저장소에서 오늘만 세 번째다(B-5, B-12-b, 여기).

### 남긴 것과 이유

- **B-5 의 「원본 크기로 보기」**: `data-zoomable` 을 떼면서 그 기능이 함께
  사라졌다. 겹침 해소가 목적이었고 대체를 넣으려면 모달에 확대를 새로 만들어야
  한다 — 지금 범위가 아니다. **의도적으로 뺐다**고 여기 적어 둔다
- **`MAX_SCALE = 8` 의 근거**: 주석이 「원본 1536 의 8배」라 적었는데 실제로는
  **460 짜리 노드**에 곱해진다. 지금 모델 크기에서는 배율이 3.34 라 안 닿는다.
  면적 기준으로 바꾸는 것이 옳지만 도달하지 않는 값이라 다음으로 미룬다
- **react-rnd 의 `bounds="parent"` + `scale` 조합**: 라이브러리 내부라 코드로는
  판단이 안 된다. 좁은 창에서 레이어를 오른쪽 끝까지 끄는 확인이 남았다

- 웹 2,927 통과 / 6 skip. 타입 0건
- 브라우저 회귀: `w6-preview-browser`·`w6-canvas-browser` 둘 다 그대로 통과

## W5 — 검수 (D-11-a·D-11-b·K-10·K-11)

**안 본 것을 통과로 적던 자리 넷을 고쳤다.**

### 무엇이 잘못됐나

| 항목 | 증상 |
|---|---|
| D-11-b·K-10 | `normalizeReview` 가 모르는 항목을 버린다. 모델이 엉뚱한 이름만 내면 `items: []` 가 되는데, **fail 이 하나도 없으니 통과처럼 보였다.** 벌점도 0 이라 재작성 루프가 그 빈 결과를 「가장 좋은 것」으로 채택했다 — **아무도 안 본 구성안이 이겼다** |
| D-11-a·K-11 | `runQaGate` 는 호출·파싱이 실패하면 빈 결함을 돌려준다(fail-open). 부르는 쪽은 `blocking.length === 0` 만 보고 **`passed: true`** 로 내보냈다. 한 번도 검사 안 한 그림이 통과 도장을 받았다 |

### 고친 것

- `reviewCompleteness()` — 일곱 항목이 정확히 한 번씩 왔는지 본다. 모르는 값·중복·
  누락은 전부 `incomplete`, 못 받았으면 `unavailable`
- **안 본 항목을 fail 보다 무겁게 센다**(`missing * 1000`). 빈 심사가 「fail 하나
  있는 온전한 심사」보다 좋아 보이던 것을 뒤집었다
- `needsRevision` 이 불완전한 심사도 다시 받게 한다
- `normalizeReview` 가 **몇 개를 버렸는지**(`droppedCount`)와 **모양이 틀렸는지**
  (`malformed`)를 남긴다. 안 남기면 「전부 엉뚱한 이름」과 「아무것도 안 냄」이
  같은 빈 목록이 된다
- `qaStatusOf()` — `passed | failed | review_required | unavailable`.
  **fail-open 자체는 맞다**(검수가 흔들린다고 이미 값을 치른 그림을 버릴 수 없다).
  다만 그 사실을 숨기지 않는다. 경고(minor)는 지금까지대로 통과다
- 화면이 「검수 못 함」을 보여 준다. 경고 뱃지와 **따로** 둔다

### 덤으로 잡은 것 — 재작성이 앞 결과를 날렸다

시험을 고치다 드러났다. 심사가 불완전해 재작성을 돌렸는데 **그 호출이 빈 응답을
받으면 던져서, 이미 만들어 둔 멀쩡한 구성안까지 함께 사라졌다.** 사용자는 몇 분을
기다린 끝에 「구성안을 만들지 못했습니다」만 보고 값도 이미 치렀다. 바로 아래
`runReview` 가 실패해도 진행하는 것과 같은 판단으로 고쳤다.

### 옛 시험 셋이 결함을 고정하고 있었다

| 시험 | 무엇을 고정했나 |
|---|---|
| 「응답이 통째로 망가져도 죽지 않는다」 | `needsRevision === false` 를 기대 — **「죽지 않는다」를 「통과다」로 적었다** |
| 「QA 호출이 실패하면 fail-open」 | `qa.passed === true` 를 기대 |
| 「심사 비교」 | 가짜 기준 이름(`c0`·`c1`)으로 **두 개만** 만들어, 다섯을 안 봤는데 「전부 통과」로 읽혔다 |

셋 다 뜻을 바로잡았다. 첫 둘은 **그림은 그대로 주되 상태만 사실대로 적는다**로,
셋째는 실제 기준 이름을 쓰도록.

변이 5종(불완전을 통과로·빈 심사 벌점 0·QA 못 돌린 것 무시·늘 통과로 적음·재작성
실패가 앞 결과 날림)이 모두 잡힌다.

- pdp-core 655 통과. 웹 2,930 통과 / 6 skip. 타입 0건

## W5 이어서 — 승인 원고와 정체성 대조 (K-12·U-10·U-02)

### K-12 — 그린 것과 검사한 것이 달랐다

생성과 QA 가 **각자 다른 목록**을 보고 있었다. 양쪽이 반대로 어긋나 있었다.

| 문구 | 그림에 | QA 는 | 결과 |
|---|---|---|---|
| 신뢰문구 | **그린다** | **안 봤다** | 그 한 줄이 「승인 안 된 글자」로 잡혀 **멀쩡한 그림을 다시 만들었다.** 값도 두 번 나갔다 |
| CTA | **안 그린다**(2026-07-30 결정) | **승인 원고로 봤다** | 없는 문구를 기준으로 삼았다 |

`approvedCopyOf()` 하나를 두고 QA 가 그것을 쓴다. 목록을 두 벌 두면 한쪽만
고치는 날이 오고, 그날 QA 는 엉뚱한 것을 잡는다.

**글자 없는 그림(`editable`)에는 승인 원고가 없다.** 사진만 만들고 카피는
편집기에서 얹는 모드라, 원고를 들이대면 QA 가 「제목이 안 보인다」를 결함으로
잡는다 — 안 그리기로 한 것인데.

### U-10 — 그림에 그려지는 글자인데 고칠 칸이 없었다

신뢰문구는 망설임을 덮는 한 줄이고, 장점만 나열된 페이지를 광고가 아니게
만드는 자리다. 그런데 **구성 확인 화면에 그 칸이 없어서** 사용자는 자기
페이지에 뭐라고 적힐지 보지도 고치지도 못한 채 생성 버튼을 눌렀다.
보여 주지 않으면 승인이 아니다.

`trust_or_objection_line` 은 이미 편집 가능한 슬롯이었다 — 칸만 없었다.

### U-02 — 계획만 만들고 배선은 보류한다 (사용자 결정)

정체성 검증(`pdp_person_check`)이 **업로드한 인물 사진에만** 돌고 있었다.
저장된 캐릭터를 골라 그리면 「같은 사람인가」를 아무도 안 봤고, 제품은 대조가
아예 없어 **라벨이 바뀌어도 통과**했다.

`identityCheckPlan()` 으로 「무엇을 대조할지」를 정하는 로직과 시험을 만들었다.
업로드 사진이 캐릭터를 밀어내는 규칙(그림에 그 사람이 나오므로), 캐릭터는 첫
장만 보는 규칙(각도마다 부르면 값이 몇 배)도 함께 담았다.

**배선은 안 했다.** 대조는 그림 한 장마다 LLM 호출이 한 번 더 붙어 섹션당
비용이 최대 두 배가 된다. 사용자에게 물었고 「지금은 계획만」으로 결정됐다.
켤 때는 이 함수를 `pdp.service.ts` 의 `validateGeneratedImage` 자리에 이으면 된다.

설계 §10.2 의 한계도 함수 머리말에 적어 두었다 — **이 대조는 「달라 보인다」를
잡는 것이지 「똑같다」를 증명하는 것이 아니다.**

- 변이 6종(신뢰문구 제외·글자없는 그림에 원고·QA 가 CTA 복귀·캐릭터 무시·제품
  무시·편집 칸 제거)이 모두 잡힌다
- pdp-core 674 통과. 웹 2,936 통과 / 6 skip. 타입 0건

## W5 이어서 — 사진 경로의 근거 게이트 (K-09·U-12)

### 두 겹으로 새고 있었다

근거 검사(`verifyEvidenceStructure`)가 **글 경로에서만** 돌았다. 사진 경로는
같은 스키마로 `evidence` 를 받으면서 아무도 안 봐서, 지어낸 인용·금지된 주장·
안 채운 질문이 그대로 통과했다.

그런데 **검사를 붙여도 안 잡혔다.** 파고드니 더 앞에서 새고 있었다 —
`normalizeSection` 이 필드를 하나씩 나열하는데 `evidence` 와 `evidenceVersion`
이 그 목록에 없어, **모델이 근거를 보내도 통째로 버려졌다.** 볼 것이 없으니
검사가 할 일도 없었다.

같은 사고가 그 함수 바로 아래 주석에 이미 적혀 있다 — 「필드를 하나씩 나열하지
않는다. 실제로 그렇게 잃었다(2026-07-30)」. 그때는 레퍼런스와 캐릭터였고 이번엔
근거다.

### 고친 것

- `normalizeSectionEvidence()`·`normalizeCopyTarget()` 을 `pdp.evidence` 로 빼서
  **두 경로가 같은 손질을 쓴다.** 글 경로에만 있던 비공개 함수였다
- 사진 경로가 근거를 보존한다(`normalizeSection`)
- 구성안이 나온 뒤 **같은 검사를 돌리고 같은 정책으로 메운다**(U-12 의
  「사진·글 모드에서 정책이 다름」이 이것으로 함께 풀린다)
- **모르는 `kind` 는 `sample` 로 떨어뜨린다.** `quoted` 로 두면 「원문에 있다」는
  주장이 근거 없이 서고 그 문장이 확인 없이 나간다

### 사진 경로의 「원문」은 무엇인가

글 경로는 사용자가 친 글이 원문이다. 사진에는 그런 것이 없다. **사용자가 직접
적은 것**(판매자 브리프·추가 정보)을 원문으로 삼는다(`photoSourceText`).

**사진에서 읽은 것은 원문이 아니다.** 「가죽으로 보인다」는 추정이고, 그것을
인용 근거로 삼으면 모델이 본 것을 사용자가 말한 것처럼 둔갑시킨다
(설계 §9.2: 「시각 추정 재질/효능을 확인 사실로 자동 승격하지 않는다」).

### 소스 문자열로는 「결과를 쓰는가」를 못 잰다

변이로 확인했다 — 검사를 부르고 **결과를 버려도** 문자열 시험은 통과했다.
그래서 `analyzeProduct` 를 실제로 돌려 **결과물이 달라지는지** 재는 시험을
더했다. 지어낸 인용이 정책대로 처리되는지 본다.

- 변이 3종(근거를 다시 버림·검사 결과를 버림·모르는 종류를 quoted 로)이 모두 잡힌다
- pdp-core 691 통과. 웹 2,936 통과 / 6 skip. 타입 0건

## W5 이어서 — 제품을 못 읽었으면 그렇다고 말한다 (U-13)

### 재는 함수는 있었고, 아무도 안 불렀다

`isProductReadingUsable` 은 만들어 두고 **내보내기 목록에만** 있었다. 사진이
흐릿하든 제품이 안 보이든 결과는 똑같이 「완성」으로 나왔다. 사용자는 근거 없는
카피를 확인된 사실로 읽는다.

### 세 낱말인 이유

「읽었다/못 읽었다」 둘로는 부족하다.

| | 사진 판독 | 사용자가 적은 것 | 뜻 |
|---|---|---|---|
| `usable` | 쓸 만함 | — | 제품에서 출발한 카피다 |
| `thin` | 부실 | **있음** | 사용자가 적은 것에 기댄다. 확인이 필요하다 |
| `unfounded` | 부실 | 없음 | **딛고 설 것이 아예 없다** |

정말 위험한 것은 마지막이다. 그때 나오는 문장은 전부 지어낸 것이다.

**사용자가 적은 글의 품질은 재지 않는다.** 잴 수 없다. 있는가 없는가만 본다.

### 이름표만 붙이면 안 부르던 때와 같다

그래서 판단이 **그 자리에서 쓰인다** — `unfounded` 면 「예시로 채우기」를
「물어보기」로 내린다.

그 정책은 「사용자가 확인하고 고칠 것을 전제로」 한 것이다. 근거가 아예 없으면
예시가 곧 페이지 전체가 된다 — 통째로 지어낸 페이지를 건네고 검수를 사용자에게
떠넘기는 꼴이다. **내리기만 한다**: 「빼기」를 고른 사람에게 지어낸 문장을 주는
일은 없다.

내렸으면 **화면이 그 사실을 말한다.** 고른 대로 안 했는데 조용하면 사용자는
예시가 채워진 줄 알고 넘어간다.

### 쓸 만할 때는 아무 말도 하지 않는다

멀쩡한 결과에 「확인하세요」를 붙이면 모든 화면에 붙는 것과 같아진다. 그러면
정말 위험한 화면에서도 사용자가 그 문구를 넘긴다.

### 같은 사고가 초안 저장에도 있었다

`normalizeGeneratedResult` 도 필드를 하나씩 나열한다. 새 필드를 목록에 안 넣으면
조용히 버려진다 — **이번 주에만 세 번째**다(심사 결과 `review`, 근거
`evidence`, 이번 `productReadingStatus`). 초안을 다시 열면 경고가 사라져 근거
없는 카피가 확인된 것처럼 보였다.

### 문구 판단을 그리기에서 뗐다

이 저장소에는 jsdom 이 없다. 문구가 컴포넌트 안에 있으면 「쓸 만할 때 아무 말도
안 하는가」, 「내린 사실을 말하는가」를 **잴 방법이 없다.**
`product-reading-notice.ts` 가 정하고 `.tsx` 는 그리기만 한다.

### 리뷰가 잡은 것 — 고친 자리가 아무도 안 지나는 길이었다

독립 리뷰가 REQUEST CHANGES 를 냈고, **여섯 건 모두 옳았다.** 둘은 이 작업의
목적 자체를 무너뜨리는 것이었다.

**① 초안 보존이 화면 경로에서 안 먹었다.** 화면은 초안을 늘 문서로 바꿨다가
되돌려 읽는다(`draft-repository` → `documentToDraft`). 그런데 `PdpDocumentV3` 에
자리가 없어 거기서 사라졌다. **`pdp-drafts` 에 넣은 보존은 아무도 지나지 않는
길에 있었다.** 새로 쓴 왕복 시험이 통과한 이유는 저장소 계층을 건너뛰고
`savePdpDraft`/`getPdpDraft` 를 직접 불렀기 때문이다.

같은 실수를 **이번 작업 안에서 또 했다** — K-09 에서 「소스 문자열로는 결과를
쓰는가를 못 잰다」를 배워 놓고, 이번엔 「저장소를 직접 부르면 화면이 쓰는 길을
못 잰다」에 걸렸다. 시험이 지나는 길과 사용자가 지나는 길이 다르면 시험은 아무
말도 하지 않는다. 이제 화면을 실제로 띄워 `ScenarioEditor` 가 **받는 값**을
본다(`draft-ui.test.tsx`, v3 켬/끔 두 갈래).

**② 화면이 하지 않은 일을 했다고 말했다.** 「빈칸으로 두었습니다」라고 단정했는데,
내린 정책은 근거 검사에 걸린 섹션에만 적용된다 — `verifyEvidenceStructure` 는
근거 딱지가 없는 섹션을 통째로 건너뛴다. **모델이 딱지를 안 붙이면 한 칸도 안
비워진다.** 그 상태에서 「치웠습니다」를 띄우면 사용자는 위험한 문장이 사라진 줄
알고 그대로 발행한다 — **원래 문제보다 나쁘다.**

그래서 **정한 것이 아니라 한 것을 적는다**(`copyGapOutcome`):

| 칸 | 뜻 |
|---|---|
| `requested` | 사용자가 고른 것 |
| `applied` | 실제로 쓴 것 |
| `cleared` | **실제로 비운 칸 수** |

화면은 `applied !== requested && cleared > 0` 일 때만, 그것도 **몇 곳인지**
말한다.

**③ 내림 규칙이 두 벌이었다.** 화면이 현재 토글값으로 「내렸다」를 되짚었다.
사용자는 결과를 본 뒤에도 정책을 바꿀 수 있어, 바꾸면 없던 말이 뜨거나 있던
말이 사라졌다. 이제 서버가 적은 것만 본다. 규칙도 한 벌로 돌아왔다.

**④ 배선 시험이 문자열 대조라 빈 배선을 못 잡았다.** `toContain("productReadingStatus")`
는 타입 선언 줄만 있어도 통과한다. `ScenarioEditor` 를 mock 해서 **받은 값**을
검사한다.

**⑤ 「대상: 30대 여성」한 줄이 근거가 됐다.** 제품에 대한 근거는 0인데 보호가
꺼졌다. 화면 흐름상 가장 채우기 쉬운 칸이라 구멍이 늘 열려 있었다. 이제
**제품을 말하는 칸만** 센다(`productFactText`: 특징·차별점·추가 정보). 인용을
대조하는 원문은 그대로 전부 잇는다 — **두 물음은 다르다.**

**⑥ 되살린 값에 허용값 검사가 없었다.** 이웃 필드는 전부 화이트리스트를 건다.

- 변이 6종이 모두 잡힌다 — 문서 왕복에서 다시 버림 · 안 비웠는데 치웠다고 함 ·
  대상 칸을 근거로 셈 · 채운 칸도 비운 것으로 셈 · 고른 정책을 한 일로 적음 ·
  화면에 안 넘김
- pdp-core 715 통과. 웹 2,953 통과 / 6 skip. 타입 0건

## W7 — 몇 장인지는 상품이 정한다 (U-14)

### 프롬프트가 모든 상품에 같은 옷을 입혔다

| 고정 조건 | 어디 |
|---|---|
| 「5~6개의 핵심 섹션」 | 사진 경로 |
| 「섹션은 4~7개」 | 글 경로 |
| 「베네핏은 3개 고정」 | 사진 경로 |
| 「bullets 한국어 3개」 | 섹션 템플릿 |
| 「리뷰 섹션은 … 후기 카드 6~12개 우선」 | 사진 경로 |
| 「첫 번째 섹션은 반드시 매력적인 모델이 제품과 함께 연출된 컷」 | 사진 경로 |

할 말이 세 가지인 제품에도 여섯 장을 요구하면 **나머지 세 장은 채우려고
지어낸 것**이 된다. 그 자리에서 나오는 문장이 「사용 후 일상이 조금 더
가벼워집니다」 같은 것들이고, 같은 프롬프트에 그런 문구를 하나씩 금지한 흔적이
남아 있다. **증상을 막았을 뿐 원인은 장수 강제였다.**

### 후기 지시는 자기 자신과 부딪혔다

「후기 카드 6~12개 우선」 바로 아래에 「근거 없는 효능·인증·수치·**후기 개수**를
만들지 말 것」이 있었다. 한 프롬프트 안에서 서로 반대말을 하고 있었다.
이제 **판매자가 알려준 후기가 있을 때만** 그 섹션을 만든다.

### 풀면 강제를 코드로 옮겨야 한다

지금까지 장수를 막는 것은 **프롬프트 문구뿐**이었다 — 코드에는 상한이 한 줄도
없었다. 문구는 부탁이지 강제가 아니다. 모델이 서른 장을 내놓으면 서른 장이
그대로 생성 대기열에 들어가고 **한 장마다 값이 나간다.**

그래서 장수를 푸는 대신 상한을 코드로 지킨다 — `MAX_PLANNED_SECTIONS = 10`,
두 경로의 파싱 지점에서 `clampSections`. 최소 장수는 강제하지 않는다
(설계 §9.1: 「최소 1섹션도 의도된 구성이라면 허용」).

### 하한으로 지키던 것을 이름으로 지킨다

전에는 「4개까지 허용하면 반론이 가장 먼저 빠진다」는 이유로 하한을 5로 올렸다
(기존 결정). 그런데 **장수는 반론이 있는지 확인하지 않는다** — 다섯 장을 만들되
전부 자랑인 페이지도 그대로 통과한다. 늘어난 것은 길이뿐이었다.

보호를 두 겹으로 옮겼다.

1. 프롬프트가 **반론 섹션을 이름으로** 요구한다(그대로 둠)
2. `sectionPlanGaps` 가 **결과물에 망설임을 다루는 문장이 있는지** 실제로 본다.
   없으면 화면이 말한다

시험도 바꿨다 — 「하한이 5인가」 대신 「다섯 장이지만 반론이 없는 구성이 잡히는가」.

### 막지 않는다. 말할 뿐이다

적은 장수가 의도된 구성일 수 있다. 그리고 `SectionPlanGaps` 는 **지금 화면의
구성안**을 본다 — 서버가 준 값이 아니라. 사용자가 아래에서 고치면 저절로
사라진다.

### 리뷰가 잡은 것

통과(APPROVE) 였지만 네 가지를 고쳤다.

**① 글 경로는 보호가 실제로 얇아졌다.** 「반론 섹션은 반드시 넣는다」는 사진
경로 프롬프트에만 있었다. 글 경로가 잃은 것은 4~7장 하한이고, 새로 생긴 것은
막지 않는 화면 안내뿐이었다 — 위 문단의 「이름으로 요구한다(그대로 둠)」는
사진 경로에만 해당하는 말이었다. **글 경로에도 같은 한 줄을 넣었다.**

**② 「부른다」와 「결과를 쓴다」를 또 문자열로 쟀다.** `toContain("clampSections(")`
는 그 낱말이 파일 어딘가에 있다는 것만 말한다. 넘치는 응답(14장)을 실제로
넣어 **두 경로 모두 10장이 나오는지** 본다. 변이로 확인했다 — 부르고 결과를
버리면 이제 잡힌다.

**③ 화면이 그 상자를 띄우는지 아무도 안 쟀다.** 컴포넌트 시험은 직접 렌더할
뿐이라, `ScenarioEditor` 의 배선 한 줄을 지워도 전부 통과했다. 인자까지 한
문자열로 본다.

**④ 내 변경이 어긋나게 만든 주석** 둘(`api/library` 의 「보통 4~7장」)을 고쳤다.

### 남긴 판단

**상한 10을 무엇 이하로 잡았나.** 설계는 「현행 전체 요청 상한 이하」라고 했는데
저장소의 후보가 둘이다 — 라이브러리 한 작업 20장, 요청당 묶음 2~6장.
**라이브러리 20장을 기준으로 삼았다.** 묶음 상한은 한 번에 보내는 수라 화면이
알아서 쪼개고(`chunkForModel`), 페이지 전체 장수와는 다른 물건이다.

**자르는 것을 알리지 않는다.** 14장 중 4장을 버려도 사용자는 모른다. 빈자리
정책을 내릴 때와 달리 **여기서 덮은 것은 사용자의 선택이 아니라 모델의
과욕**이다. 사용자는 14장을 요청한 적이 없다. 알림을 붙이면 사용자가 하지도
않은 일을 설명받는다.

- 변이 8종이 모두 잡힌다 — 상한 안 지킴 · 두 경로가 자르기를 안 부름 · **두 경로가
  부르고 결과를 버림** · 모든 섹션에 신뢰 문구 요구 · 화면이 안 그림 ·
  **화면 배선만 제거** · 글 경로 고정 장수 되돌림
- pdp-core 739 통과. 웹 2,963 통과 / 6 skip. 타입 0건

## W2/W5/W7 — 페이지 전체가 같은 디자인을 따른다 (U-15)

### 통일 장치가 한쪽 경로에만 있었다

섹션 이미지는 **서로를 모른 채 각각 생성된다.** 앵커 이미지에는 글자가 없어
서체를 전달할 수도 없다. 그래서 무엇을 공유할지 한 번 정해 모든 섹션에 실어
보내야 한다 — 안 그러면 섹션마다 서체가 바뀌고 색이 바뀌고 등장인물이 바뀐다.

그 장치(`designSystem`)가 **글 경로에만 있었다.** 사진 경로는 섹션마다
`style_guide` 를 제각각 적었다. 프롬프트가 「전체 통일 스타일」이라 적어 두긴
했지만, **여섯 섹션이 각자 자기 문장을 쓰면 통일될 수가 없다.**

### 새 섹션은 형제를 베꼈다

`createSectionFor` 가 `sections.find(s => s.style_guide)?.style_guide` 로
형제 것을 통째로 복사했다. 그 형제를 사용자가 고치거나 지우면 **계승이 조용히
끊긴다** — 새 섹션만 다른 서체·다른 인물로 만들어지고, 그 사실은 **이미지가
나온 뒤에야** 보인다. 한 장에 값이 드는데 그때는 이미 늦다.

이제 공용 디자인을 **직접** 받는다. 형제가 옛 디자인을 들고 있어도 지금 것을
받는다. 공용 디자인이 없는 옛 초안은 전처럼 형제에게서 물려받는다.

### 싣기는 멱등이어야 한다

다시 기획할 때마다 같은 문장이 쌓이면 프롬프트가 길어지고, 디자인을 바꿨을 때
**옛 서술과 새 서술이 함께 남아** 모델이 어느 쪽을 따를지 알 수 없게 된다.
그래서 붙이기 전에 표지(`DESIGN_SYSTEM_MARKER`)부터 뒤를 걷어낸다.

### 표지만 보면 옛 디자인이 통과한다

`sectionsMissingDesignSystem` 은 표지가 있는지가 아니라 **지금 디자인의 서술과
같은지**를 본다. 표지만 보면 옛 서체·옛 인물을 든 섹션이 「받았다」로 통과하고,
그 섹션만 다른 모습으로 만들어진다.

### 왕복은 지금도 살아남지만 묶어 뒀다

문서·초안 왕복은 펼치기(`...doc.blueprint`)로 옮겨서 `designSystem` 이
살아남는다. 그런데 **이번 세션에서 필드를 하나씩 나열하는 자리에 세 번
걸렸다**(심사 결과 · 근거 · 판독 상태). 누군가 이 자리도 나열로 바꾸는 날 새
섹션이 조용히 다른 서체로 만들어지기 시작한다 — 변이로 확인했고, 나열로 바꾸면
시험이 잡는다.

### 리뷰가 잡은 것 — 진단해 놓고 그 줄을 안 고쳤다

**① 프롬프트가 `style_guide` 를 두 번, 반대로 정의했다.** 앞에
`DESIGN_SYSTEM_RULES` 를 놓아 「서체·색·등장인물은 designSystem 이 정한다」고
해 놓고, 뒤에서 여전히 「전체 통일 스타일 … **서체 인상**을 적을 것」이라
말했다. **뒤에 있는 쪽이 이긴다** — 섹션마다 서체를 또 정하고, 그 제각각인
글이 공용 서술 **앞에** 남았다. 통일하려고 만든 장치가 통일을 못 시켰다.

부끄러운 것은 두 가지다. 그 자리 주석이 **이미** 「`style_guide` 설명이 한
프롬프트에 두 번, 다른 어휘로 있으면 안 된다 … 뒤에 있는 쪽이 이긴다」고
적어 뒀고, 새 모듈 머리말에는 내가 **바로 그 문장을 진단해 놓고** 고치지
않았다. 이제 섹션 필드는 「그 섹션만의 연출」로 좁혔고, **정의가 한 벌인지**
재는 시험을 붙였다.

**② 컷 타입 우선이면 공용 디자인이 통째로 사라졌다.** `style-first` 는
`style_guide` 를 버리고 장면을 새로 짠다. 그 안에 페이지 공용 디자인이 함께
실려 있어서, **한 섹션만 컷 타입 우선으로 두면 그 섹션만 다른 서체·다른
사람으로** 만들어졌다 — U-15 가 막으려던 바로 그 실패인데 새 검수는 구성안
글자만 봐서 「전부 받았음」이라고 말했다. 토글은 섹션마다 따로다.

컷 타입이 덮어야 하는 것은 그 섹션의 **구도**지 페이지 전체의 서체가 아니다.
`designSystemPartOf` 로 공용 서술만 남긴다.

**③ 경고는 뜨는데 고칠 길이 없었다.** `style_guide` 는 화면 어디에서도 못
고친다. 「N개 섹션이 공용 디자인을 받지 않았습니다」는 읽고 넘길 수밖에 없는
말이었고, 바로 아래 「아래에서 직접 고칠 수 있습니다」는 **거짓말**이었다.
「모든 섹션에 페이지 디자인 적용」 버튼을 붙였다 — 값이 안 드는 순수 함수다.

**④ 스키마와 규칙 문구가 아직 두 벌이었다.** 글 경로에 지역 사본이 남아,
사진 경로에만 「사람이 안 나오는 페이지면 `cast` 는 빈 문자열」이 있었다(U-14 와
어긋난다). 지웠다. 시험도 `toContain("pdp.design-system")` 로 **import 줄만**
보던 것을 공용 상수 자체를 대조하게 바꿨다.

**⑤ 편집창이 열리는 날의 함정**을 함수 주석에 남겼다. 표지는 문장 끝에 붙으므로
사용자가 그 뒤에 적으면 다음 `applyDesignSystem` 한 번에 사라진다. 지금은
`style_guide` 입력이 없어 안전하다(`PdpEditor` 는 읽기만 한다).

- 변이 10종이 모두 잡힌다 — 옛 서술을 안 걷어냄 · 표지만 보고 통과 · 사진 경로가
  안 실음 · 새 섹션이 다시 형제만 베낌 · 화면이 공용 디자인을 안 넘김 ·
  문서 왕복을 나열로 바꿈 · **두 번째 정의를 되돌림** · **컷 타입 우선이 공용
  디자인을 버림** · **컷 타입 우선이 섹션 연출까지 되살림** · **버튼이 아무것도
  안 고침** · **글 경로가 공용 규칙을 안 실음**
- pdp-core 764 통과. 웹 2,979 통과 / 6 skip. 타입 0건

## W2/W7 — 전략을 고치는 것과 구성을 다시 짜는 것 (U-11)

### 고쳐도 아무 일이 안 일어났다

「전체 전략」은 그냥 글상자였다. 고쳐도 **섹션은 그대로다.** 그런데 화면이 그
사실을 말하지 않아, 사용자는 전략을 고치고 다음 단계로 넘어가 **고친 전략이
반영된 줄 알고** 이미지를 만든다. 한 장에 값이 드는데 나온 그림은 옛 전략을
따른다.

반대편에는 「설정 바꿔 다시 만들기」뿐이었다. 그것은 업로드 화면까지 되돌려
구성안을 통째로 버린다 — 공들여 고친 문구도 함께 사라진다.

**고친 전략으로 구성만 다시 짜는 길이 없었다.**

### 셋으로 갈랐다

| 하는 일 | 무엇이 바뀌나 |
|---|---|
| 전략 칸 고치기 | 요약만. 섹션은 그대로 — **화면이 그렇게 말한다** |
| 이 전략으로 구성 다시 만들기 | 그 전략에서 출발해 섹션을 새로 짠다 |
| 설정 바꿔 다시 만들기 | 업로드로 돌아가 처음부터 |

설계 §4.2 가 「전체 전략 수정은 요약 수정이다. 섹션 재설계는 별도의 동작이며
이전 revision을 보존한다」고 적은 자리다.

### 지시는 맨 앞에 둔다

긴 규칙 뒤에 붙이면 묻힌다. 심사 지적(`buildRevisionDirective`)을 맨 앞에 두는
것과 같은 이유다. 그리고 「조금 고치라」가 아니라 **「이 전략에서 출발해 새로
짜라」**고 말한다 — 앞엣말이면 모델이 옛 구성을 손보는 쪽으로 간다.

전략 칸은 사용자가 자유롭게 쓰는 글이라 프롬프트 구분자(`===`)를 지운다.

### 되돌릴 수 있다

재기획 직전 구성을 들고 있다가 한 번 되돌릴 수 있다. **초안에는 저장하지
않는다** — 화면을 다시 열면 「방금 다시 짰다」는 맥락이 사라지고, 그때 되돌리기
단추만 남으면 무엇으로 돌아가는지 알 수 없다.

### 지나가다 만든 결함 하나

`onClick={handleAnalyze}` — 인자를 받게 고친 순간 **클릭 이벤트가 첫 인자로
들어가** 전략 지시가 될 뻔했다. 시험도 처음에는 못 잡았다. 인자 없이
`onClick()` 을 불렀기 때문인데, **브라우저는 늘 이벤트를 넘긴다.** 시험이 실제
클릭처럼 이벤트를 넘기게 고치니 그제야 잡혔다.

### 리뷰가 잡은 것 — 되돌리기가 남의 작업을 덮을 수 있었다

**① `previousPlan` 이 어느 작업의 것인지 몰랐다.** A 를 다시 짠 뒤 B 를 열면
**A 의 되돌리기 단추가 살아 있고**, 누르면 B 위에 A 의 유료 이미지가 앉는다.
리뷰가 화면을 띄워 실측으로 잡았다. 작업을 갈아 끼우는 두 자리
(`resetWorkspace`·`handleLoadDraft`)에서 버린다.

작업 id 로 대조하는 방법도 해 봤다가 **뺐다** — 저장 안 한 작업이 자동 저장으로
id 를 받는 순간, 같은 작업인데도 단추가 사라진다.

**② 되돌리기만 보관본을 안 팠다.** 다시 짠 뒤 이미지를 만들고 돌아와 되돌리면
**그 유료 이미지가 통째로 사라진다.** 다른 모든 덮어쓰기 자리는
`preserveBeforeReplacement` 를 지나는데 여기만 빠져 있었다.

**③ 실패하면 엉뚱한 화면에 떨궜다.** 보관 실패 시 늘 편집기로 보냈는데, 구성
화면에서 누르는 길이 생기며 어긋났다. 게다가 오류 문구는 업로드 화면에서만
그려져 편집기에서는 **아무 설명이 없다.** 왔던 화면으로 돌려보낸다.

**④ 새로고침하면 안내가 거짓말이 됐다.** 「이전 구성으로 되돌릴 수 있습니다」는
초안에 저장되는데 단추는 안 남는다. 「저장된 작업에 보관했습니다」로 바꿨다 —
그쪽이 사실이고, 실제 보존은 보관 초안이 한다.

**⑤ 전략 칸에 길이 제한이 없었다.** 서버는 2000자에서 막는다. 긴 글을
붙여넣으면 설명 없는 400 을 만난다. `MAX_STRATEGY_LENGTH` 를 **화면과 서버가
함께** 쓴다.

**⑥ 안내문이 사진 경로에만 떴다.** 「전략만 고치면 섹션은 그대로」라는 사실은
글 경로에서도 참인데, 단추 조건 안에 들어 있어 **글 경로 사용자는 그 사실
자체를 못 봤다.** U-11 이 고치려던 오해가 거기 그대로 남아 있었다.

**⑦ 주입 소독을 한 벌로.** `===` 만 지웠는데, 이 저장소의 지시문이
`[사용자 추가 정보]:` 꼴이라 **줄머리 라벨을 흉내내면** 사용자 글이 시스템
지시로 읽힌다. `sanitizePromptText` 로 합쳤다. 막아 주는 것이지 증명해 주는
것이 아니라, 마지막 지시는 사용자 글 **뒤**에 둔다.

**⑧ 빨개질 수 없는 시험 하나를 지웠다.** `toContain("onRestorePreviousPlan={")`
는 삼항을 뒤집어도 통과한다. 실제 보장은 화면을 띄우는 시험이 한다.

- 변이 9종이 모두 잡힌다 — 전략을 기획에 안 넘김 · 빈 전략도 지시문을 만듦 ·
  요청 조립이 빠뜨림 · 이전 구성을 안 들고 있음 · 늘 들고 있음 · 클릭 이벤트가
  전략으로 샘 · **초안을 갈아 끼울 때 안 버림** · **되돌리기가 보관을 건너뜀** ·
  **라벨 흉내 주입을 안 막음**
- pdp-core 774 통과. 웹 2,998 통과 / 6 skip. 타입 0건

## W2/W3 — 「제품 보존」을 꺼도 제품 사진은 간다 (U-03)

### 토글이 입력을 지웠다

`shouldSendAnchor` 는 「레퍼런스가 있고 제품 보존을 끄면」 `false` 를 돌려줬다.
그러면 **제품 사진이 아예 안 실린다.** 모델은 레퍼런스만 보고 **제품을
지어낸다.**

화면은 그것을 이렇게 말했다 — 「대신 섹션마다 제품 모습이 **조금씩 달라질 수
있습니다**」. 사실은 「**다른 제품이 나옵니다**」였다.

설계 §9.1(U-03): 「실물 product 역할의 **보존 불변**」.

### 원래 근거는 여전히 참이다

앵커를 뺀 데는 실측 근거가 있었다 — 참조가 둘이면 모델이 절충한다. 배경과
글자는 레퍼런스(네이비+옐로)를 따랐는데 **제품 라벨만 앵커 성향으로 크림색이
남았다.** 앵커를 빼니 라벨까지 레퍼런스를 따랐다.

그 문제는 사라지지 않았다. 다만 **해법이 틀렸다** — 절충을 없애려고 입력을
지웠고, 그 대가로 제품 자체를 잃었다. 지금은 **지시로** 푼다.

| 토글 | 역할 | 뜻 |
|---|---|---|
| 켬 | `identity` | 형태·색·재질·라벨을 모두 지킨다 |
| 끔 | `shape-only` | 색·마감은 레퍼런스를 따르고, **형태와 라벨 글자는 지킨다** |

**형태만이어도 라벨 글자는 지킨다.** 글자가 바뀌면 그건 다른 제품이고, 없는
브랜드를 지어낸 것이 된다.

**레퍼런스가 없으면 끄든 켜든 `identity` 다.** 따를 디자인이 없는데 색을 풀면
제품이 아무 색이나 된다.

### 기본값은 지킨다

`buildReferenceRoleDirective` 에 역할을 안 주면 `identity` 다. 옛 호출자가
**조용히 보존을 잃으면 안 된다.** 변이로 확인했다 — 기본값을 `shape-only` 로
뒤집으면 시험 5건이 빨개진다.

### 옛 동작을 지키던 시험 둘을 바꿨다

지우지 않고 **반대 방향으로 다시 썼다.** 왜 바뀌었는지(원래 근거는 참이지만
해법이 틀렸다는 것)를 시험 주석에 남겼다.

### 리뷰가 잡은 것 — 앵커가 늘 실물인 것은 아니었다

**① 글 경로의 앵커는 우리가 만든 대표 이미지다.** 「언젠가 실물 없는 입력이
들어오면」이라 주석을 달았는데 **이미 들어와 있었다** — `TextModeFlow` 가
`originalImage` 자리에 keyVisual 을 넣고, 그 뒤 단계는 사진 경로와 완전히 같다.

그래서 제 변경은 강의·코칭의 대표 이미지를 「판매 중인 제품, **라벨 글자까지**
철자 그대로 지켜라」로 선언하게 만들었다. 거기엔 헤드라인 글자와 임의의 소품이
박혀 있다 — **그 글자가 페이지 전체에 되풀이된다.** 전에는 그 경우 앵커가 빠졌으니
**새로 만든 손상**이다.

세 번째 역할 `mood-only` 를 두고, 앵커가 무엇인지(`anchorKind`)를 화면에서
서버까지 실어 보낸다. 그리고 `shouldSendAnchor` 에 **진짜 판단이 돌아왔다** —
실물 사진은 언제나 보내고, 만들어 낸 대표 이미지는 디자인 레퍼런스가 있으면
보내지 않는다(둘 다 보내면 원래 실측이 말한 그 절충이 일어난다).

**② 화면이 미검증 동작을 사실로 단정했다.** 「제품의 색과 마감을 레퍼런스에
맞춥니다」라고 썼는데, **`shape-only` 가 실제로 색을 넘기는지는 실측하지
않았다.** 오히려 같은 패키지의 2026-07-30 실측은 반대 방향이다 — 「정체성은
첨부된 이미지가 지킨다. 설명이 필요 없다」, 제품 보존 지시가 **아예 없는
조건에서도** 정체성이 유지됐다.

즉 이 변경은 **측정된 손상(제품 날조)을 미측정 가설과 맞바꿨다.** 맞바꾼 것은
옳다고 본다 — 최악이어도 「레퍼런스 색이 덜 반영된다」에 그친다. 다만 **고친
이유가 「화면이 사실 아닌 말을 했다」였는데 방향만 바꿔 같은 종류의 말을 쓸
뻔했다.** 문구를 「조정될 수 있습니다」로 낮추고, 한계를 두 머리말에 적었다.

**③ `shouldSendAnchor` 가 장식이었다.** 게이트를 통째로 걷어내도 787건이 전부
통과했다 — 판단처럼 보이는데 판단이 아니었다. ①을 고치며 살아났고, 이제
걷어내면 잡힌다.

**④ 새 규칙 블록이 4줄 중 3줄 무시험이었다.** 「형태만 지킨다」는 역할인데
**「형태」를 재는 시험이 0건**이었다. 기존 블록처럼 문장째로 재는 루프를 붙였다.

**⑤ 머리말 둘이 옛 설계를 설명한 채 남았다.** 새 함수 주석만 고치고 파일
머리말은 「빼는 것이 해법」으로 읽혔다. 반대 근거(2026-07-30 실측표)와 새 가설이
**같은 화면에 있어야** 다음 사람이 오해하지 않는다.

**⑥ 옛 근거를 든 시험 주석이 한 곳 더 있었다**(세 번째 자리).

- 변이 7종이 모두 잡힌다 — 다시 앵커를 뺌 · 역할을 안 넘김 · 라벨 글자를 풂 ·
  레퍼런스 없이도 색을 풂 · 역할 기본값을 뒤집음 · **실루엣·비율 줄 삭제** ·
  **카메라 각도 줄 삭제** · **게이트를 걷어냄** · **대표 이미지를 identity 로** ·
  **화면이 글 경로를 실물로 보고함** · **page wire 가 안 실음**
- pdp-core 791 통과. 웹 3,001 통과 / 6 skip. 타입 0건

## W3 — 둘 다 골랐으면 물어본다 (U-04)

### 조용히 하나를 버렸다

인물 사진을 올리고 저장 캐릭터도 고르면, 서버가 **말없이 업로드 쪽을 쓰고
캐릭터를 버렸다.** 코드 주석은 「업로드한 사진이 우선이다 — 사용자가 방금 고른
쪽이다」라고 적어 뒀는데, **구성안 화면에서는 캐릭터가 더 나중일 수도 있다.**

어느 쪽이든 문제는 같다 — 사용자는 자기 선택이 무시된 것을 **이미지가 나온
뒤에야** 안다. 그리고 한 장에 값이 든다.

설계 §6: 「명시적으로 적용 대상을 선택하게 한다. **우선순위로 하나를 조용히
버리지 않는다.**」

### 「둘 다 쓰기」는 선택지가 아니다

2026-07-30 실측에서 얼굴 참조가 둘이면 모델이 절충해 **제3의 인물**을 만들었다.
그래서 하나만 쓰는 것 자체는 맞다. 틀린 것은 **말없이 고르는 것**이었다.

### 안 골랐을 때는 전과 같게 둔다

`resolvePersonSource` 는 선택이 없으면 업로드를 쓴다 — 지금까지의 동작이다.
**이미 저장된 초안이 조용히 달라지면 안 된다.** 대신 화면이 그 사실을 보여
준다(「고른 캐릭터는 이 페이지에 나오지 않습니다」).

**없는 것을 고르면 있는 쪽을 쓴다.** 캐릭터를 고른 뒤 그 캐릭터를 빼도 옛
선택이 남아 있을 수 있는데, 그때 아무도 안 나오면 「사람이 나오는 섹션」이
통째로 빈다.

### 배선은 다섯 칸

화면 상태 → 구성안 화면(고르기) → 편집기 → `page-wire` → zod → 서버.
어느 한 칸이 빠지면 고르기가 아무것도 안 바꾼다. **변이로 칸마다 확인했다.**

### 리뷰가 잡은 것 — 고른 대로 그려 놓고 틀린 답안지로 채점했다

**① 주 경로가 실제로는 실패했다.** 참조를 담는 자리만 고치고, **프로필
뽑기·재시도 상한·동일 인물 검증 세 자리는 옛 조건을 그대로** 뒀다. 그래서
캐릭터를 고르면 그림은 캐릭터로 그려 놓고 검증은 **업로드 얼굴과 대조** —
당연히 불일치라 **세 장을 태우고 요청이 실패**한다. 회원 과금은 막히지만 fal
비용은 나간다.

**그리고 내 시험이 그것을 덮었다.** stub 을 `isSamePerson: true` 로 고정해서
검증이 도는지 안 도는지가 안 보였다. 더 나쁜 것은 **그 자리에 내가 적은
주석**이다 — 「업로드 인물이 있으면 프로필 뽑기와 동일 인물 검증이 함께 돈다.
그 둘이 통과해야 그림이 나오므로 한 응답으로 셋을 다 만족시킨다.」
**그 문장이 곧 버그 신고였는데 우회로 썼다.**

이제 `isSamePerson: false` 로 두고 **호출 횟수**를 잰다. 검증이 돌면 세 장이
나오므로 한 장인지 본다.

**② 안 쓸 사진을 애초에 안 넘긴다.** `buildSectionImageOptions` 의 주석이 이미
「이 섹션에 안 쓰는 사진은 아예 넘기지 않는다」고 말하는데, 캐릭터를 고른 경우가
빠져 있었다.

**③ 글 경로에 같은 충돌이 살아 있었다.** 사진 모드에서 인물을 올린 뒤 글 모드로
바꾸면 **그 사진이 남는다.** 거기서 캐릭터를 고르면 둘이 되는데 고를 칸이 없었다.
게다가 `PdpTextDraftState.personSource` 를 만들어 놓고 **읽는 곳도 쓰는 곳도
없었다** — 다음 사람에게 「여기 배선돼 있다」는 거짓 신호다. 배선했다.

**④ 화면 → 편집기 칸에 시험이 0건이었다.** 섹션 이미지는 **편집기에서만**
만들어지므로 그 줄이 빠지면 고르기가 아무것도 안 바꾸는데, 3,014건이 전부
통과했다. 진행 문서에 적은 「다섯 칸을 변이로 확인했다」에서도 이 칸만 빠져
있었다.

**⑤ 문구가 거짓이었다.** 「고른 캐릭터는 **이 페이지에** 나오지 않습니다」 —
올린 사진을 대표컷에만 쓰면(`hero-only`) 나머지 섹션에는 캐릭터가 나온다.
사용 범위를 함께 받아 갈라 말한다.

**⑥ 웹 시험이 통과인데 exit 1 이었다.** 내가 더한 시험만 `preparedImage`+
`modelImage` 를 함께 실어 초안 적용이 8틱을 넘겼고, unmount 뒤에 콜백이 깨어나
unhandled rejection 이 났다. 「3,014 통과」는 건수로는 맞지만 **명령은
실패**였다.

**⑦ `identityCheckPlan`** 의 주석이 U-04 이후 거짓이 됐고, 그 근거가 정확히 ①의
증상이었다. 같은 판단(`resolvePersonSource`)을 쓰게 했다.

- 변이 10종이 모두 잡힌다 — 서버가 다시 업로드를 강제 · 없는 것을 골라도 그대로
  씀 · 안 골랐을 때 캐릭터가 이김 · 문서 왕복이 버림 · 편집기가 안 보냄 ·
  화면이 안 띄움 · 충돌 아닌데도 띄움 · **프로필 뽑기가 옛 조건** · **안 쓸
  사진을 다시 실음** · **화면이 저장된 선택을 안 비춤**
- pdp-core 807 통과. 웹 3,018 통과 / 6 skip. **웹 exit 0**. 타입 0건
