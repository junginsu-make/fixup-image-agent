# Codex 구현 요청 프롬프트 (복사해서 붙여넣기)

아래 내용을 그대로 Codex 에 붙여 넣으세요.

---

이 저장소에서 **설계 문서와 구현 계획이 이미 확정된 기능을 구현**해 주세요.
설계를 다시 논의하지 말고, 계획대로 만들어 주세요. 다 만들면 다른 사람이 리뷰합니다.

## 저장소

`C:\Users\PC\Desktop\coding\Detail Page` — pnpm 모노레포, Next.js 15 + TypeScript(strict), vitest
운영 중인 서비스입니다(`https://page.mktinsight.kr`, AWS EC2, 회원제). 회귀를 내면 실제 사용자가 막힙니다.

**현재 브랜치:** `design/text-fidelity-controls`
**작업 브랜치:** 여기서 `feat/text-fidelity-controls` 를 새로 따서 작업하세요.

## 먼저 읽을 것 (순서대로)

1. `docs/superpowers/specs/2026-07-29-text-fidelity-controls-design.md` — **무엇을 왜 만드는가.** 정본
2. `docs/superpowers/plans/2026-07-29-text-fidelity-controls.md` — **작업 단위와 순서.** 이걸 따라가세요
3. 손댈 코드
   - `packages/pdp-core/src/pdp.text-plan.ts` (글기반 생성 전체)
   - `packages/pdp-core/src/types.ts`, `pdp.review.ts`, `pdp.sales-principles.ts`
   - `apps/web/app/create/TextModeFlow.tsx`, `TextBriefInput.tsx`, `ScenarioEditor.tsx`
   - `apps/web/app/api/pdp/plan-from-text/route.ts`, `images/route.ts`, `images/batch/route.ts`

## 배경 (짧게)

사진 없이 **글만으로** 상세페이지를 만드는 경로가 있습니다(무형 상품 — 강의·코칭·구독·SaaS).
짧은 텍스트 하나에서 6~8개 섹션의 카피를 지어내는데, **원문에 없는 사실이 섞여도 알아낼 방법이
없습니다.** `pdp.sales-principles.ts:190` 이 "근거 없는 숫자는 법적 문제"라고 적어 두고도
지키는지 검사하는 코드가 없습니다.

운영자 결정으로 **예시 수치를 채워 주는 기능은 넣습니다** — 일반 판매자는 빈 화면 앞에서
무엇을 써야 할지 모릅니다. 대신 그것이 예시임을 놓칠 수 없게 만들고, 확인 전에는 이미지로
굽지 못하게 막습니다. 설계는 이 결정을 전제로 두 번의 외부 리뷰를 거쳐 확정됐습니다.

## 지켜야 할 것

**절대 규칙**

1. **새 LLM 호출을 만들지 마세요.** 근거는 기존 구성안 생성 호출의 응답에 실려 옵니다
2. **사진으로 시작하는 경로(`analyze`)를 건드리지 마세요**
3. **기존 함수 시그니처를 바꾸지 마세요.** 새 인자는 전부 선택(optional)
4. **`prompt_en` 을 근거 대상으로 삼지 마세요.** `mergeArtDirection` 이 확인 화면 이후에
   다시 쓰기 때문입니다(설계 §4-2). 근거 대상은 `prompt_ko` 하나입니다
5. **Task 6(서버 게이트)이 끝나기 전에 Task 8(`sample` 켜기)을 하지 마세요**

**작업 방식**

- 계획의 **작업 하나씩** 진행하세요. 테스트를 먼저 쓰고, 실패를 확인하고, 구현하고, 통과를
  확인하고, 커밋합니다. 작업 하나에 커밋 하나
- 순수 함수에 `Date.now()` 를 쓰지 마세요. 시각이 필요하면 인자로 받습니다
- 객체를 수정하지 말고 새로 만드세요(스프레드)
- 파일 400줄을 넘기지 마세요
- 테스트·주석·UI 문구는 **한국어**. 기존 파일의 어투를 따르세요
- 커밋 메시지는 `<type>(scope): 한국어 한 줄` 형식. 본문에는 **왜** 그렇게 했는지 적으세요

**검증 (매 작업마다)**

```bash
pnpm typecheck   # 0
pnpm lint        # 0 (기존 <img> 경고는 그대로 둡니다)
pnpm test        # 전부 통과
```

마지막에 `pnpm build` 까지 0을 확인하세요.

## 계획에서 벗어나야 할 것 같으면

계획이 코드 현실과 안 맞는 곳이 있을 수 있습니다. 그럴 때는 **말없이 다르게 만들지 말고**,
그 작업을 멈추고 다음을 적어 주세요.

- 계획의 어느 단계인지
- 무엇이 안 맞는지 (파일:줄 근거)
- 대안 두 개와 당신의 추천

## 하지 말아야 할 것

- 계획에 없는 기능 추가 (요청하지 않은 리팩터링·"개선" 포함)
- 실패하는 테스트를 지우거나 `skip` 처리
- 검증을 돌리지 않고 완료라고 보고하기
- 배포(EC2·main 머지). **커밋과 푸시까지만** 하세요
- 커밋 메시지에 "리뷰 반영" 같은 빈 문장 쓰기

## 다 만든 뒤 보고할 것

```
## 완료한 작업
Task 1~9 중 어디까지, 각각 커밋 해시

## 검증 결과 (실제 출력을 붙여 주세요)
pnpm typecheck / pnpm lint / pnpm test / pnpm build

## 계획과 다르게 한 곳
- [무엇을] [왜] [파일:줄]

## 못 한 것과 이유

## 리뷰어가 특히 봐 줬으면 하는 곳
```

## 리뷰 예고

작업이 끝나면 **다른 에이전트가 코드를 리뷰**합니다. 그 리뷰는 다음을 봅니다.

- 설계 §7-1 의 15개 검증 항목이 실제 테스트로 존재하는지
- 게이트가 **크레딧 예약 전에** 닫히는지
- 문구를 고쳤을 때 확인이 무효가 되는지
- 모델이 만든 `user`·`acknowledgedAt` 이 정규화에서 잘리는지
- 강등이 실패 종류별로 다르게 동작하는지 (없는 target 때문에 멀쩡한 문구가 지워지지 않는지)
- 순수 함수에 시각·난수가 섞이지 않았는지

미리 그 눈으로 자기 코드를 한 번 보고 넘기면 왕복이 줄어듭니다.
