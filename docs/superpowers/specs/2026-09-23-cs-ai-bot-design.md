# CS 응답 AI 설계

2026-09-23 · 사용자 요청

> 이 시스템을 처음 사용하는 사용자들을 위해서 사용 방법이나 궁금한 문의가 있을
> 경우 자동 응답하는 AI bot을 만드려고 합니다. 기존 쳇봇과 다르게 기존은
> 정형화된 답만 해줬다면 AI bot은 이 시스템의 구조, 기능, 단계별 항목 설명 등
> 이 시스템의 상세한 것들을 사용자가 문의시 정확한 답변을 해주는 llm 기반의
> bot입니다. 사용설명서 페이지내용은 물론이고 결제, 오류났을 때 대응, 회원가입
> 탈퇴 방법까지도 모든 CS를 AI가 대응해야 하는데 가능할까요?

**결론: 가능하다. 다만 「모든 CS」는 지금 이 저장소가 가진 것만으로는 안 된다.**
막는 것은 모델이 아니라 **답할 근거가 없는 영역**이다. 이 문서는 무엇이 이미
있고, 무엇이 없고, 어떤 순서로 만들지를 적는다.

---

## 0. 요약

| | |
|---|---|
| 이미 있는 것 | 설명서 11개 주제(2,700줄) · RAG 한 벌(342줄) · 대화형 LLM 한 벌 · 업체 무관 구조화 응답 어댑터 · 비용 미터 · 메일 발송 |
| 없는 것 | **결제·환불·탈퇴·오류 대응 문서** · 문의 채널 · 봇용 사용량 칸 |
| 가장 큰 위험 | **틀린 답**. 모른다고 말하지 않는 봇은 없는 것만 못하다 |
| 두 번째 위험 | 설명서의 숫자가 **본문에 없다**(함수로 셈한다). 소스를 그대로 넣으면 봇이 `{한장당(model)}장`을 읽는다 |
| 권하는 범위 | **안내만.** 처리(탈퇴·환불·크레딧 지급)는 사람에게 넘긴다 |

---

## 1. 지금 무엇이 있는가 (코드 확인 결과)

### 1.1 설명서 — `apps/web/app/guide/`

11개 주제, 2,705줄.

| 주소 | 주제 |
|---|---|
| `/guide` | 처음 오셨다면 |
| `/guide/easy` | 쉽게(말로 만들기) |
| `/guide/image` | 다양하게(광고·포스터·이미지) |
| `/guide/cardnews` | 카드뉴스 |
| `/guide/detail-page` | 상세페이지 |
| `/guide/redesign` | 리디자인 |
| `/guide/character` | 캐릭터 |
| `/guide/ad` | 광고 소재 |
| `/guide/library` | 라이브러리 |
| `/guide/credits` | 크레딧과 모델 |
| `/guide/team` | 팀 |

**목차는 단일 출처다** — `guide/_components/topics.ts` 하나를 목차·개요 지도·
이전다음 이동이 함께 본다.

**설명서는 로그인 없이 열린다**(`guide/layout.tsx`, 미들웨어 `PUBLIC_PATHS`).
회원에게는 스튜디오 셸을, 손님에게는 공개 머리·꼬리를 두른다.

### 1.2 RAG — `packages/redesign-core/src/rag.ts` (342줄)

완성돼 있고 **지금 서버에서 켜져 있다**(`DATABASE_URL`·`OPENAI_API_KEY` 둘 다
`/etc/fixup-image-agent/app.env` 에 있다).

```
저장소   Neon Postgres + pgvector          ← Supabase 와 별개다
임베딩   text-embedding-3-small (1536)
표       knowledge_documents / knowledge_chunks
색인     ivfflat (vector_cosine_ops, lists=100)
갈래     kind = 'sales' | 'redesign'
```

함수: `ensureRagSchema` · `indexKnowledgeDocument` · `retrieveKnowledge` ·
`chunkText` · `filterByRelevance` · `deleteKnowledgeDocument` ·
`getKnowledgeStats` · `isRagConfigured`.

**이미 유사도 하한을 두고 있다**(`DEFAULT_MIN_SIMILARITY = 0.35`). 주석이
이유를 적어 뒀다 — 이 문서가 말하려는 위험과 같은 것이다.

> 하한 없이 상위 K건을 그대로 쓰면, 지식이 얇을 때 전혀 무관한 조각이
> "검증된 판매 지식"이라는 이름표를 달고 프롬프트에 들어간다.

### 1.3 대화형 LLM — `app/easy/chat.ts` · `lib/easy/chat-provider.ts`

「쉽게」 모드가 이미 대화한다. 2026-09-21 사용자 요청으로 들어갔다 —
「그냥 ChatGPT · Gemini · Claude 처럼 쓸 수 있어야 된다」.

- **말인가 주문인가**를 가르는 판단이 화면 밖 순수 모듈에 있다(`chat.ts`)
- 부르는 일은 `app/api/easy/generate/route.ts`
- 업체 가르기는 공용 어댑터(`lib/llm/structured.ts`)의
  `AnthropicStructuredProvider` / `OpenAIStructuredProvider`

**CS 봇은 이 구조를 그대로 쓴다.** 새로 만들 것은 판단 규칙과 프롬프트뿐이다.

### 1.4 사용량·비용

- `withLlmMeter` / `readLlmMeter` 가 한 요청의 글 모델 값을 모은다
- `reserveAiUsage(request, operation, units)` → `settleAiUsage(...)`
- 시간당 한도는 `lib/membership/hourly-limit.ts` 의 표

### 1.5 메일

`lib/email/approval.ts` 에 `sendApprovalEmail` · `sendConfirmationEmail` ·
`sendPasswordResetEmail`. SMTP 설정이 서버에 있다. **문의를 메일로 넘기는 길은
새로 만들 필요가 없다.**

---

## 2. 무엇이 없는가

### 2.1 답할 근거가 없는 영역

사용자가 말한 「모든 CS」 중 **문서가 아예 없는 것들**이다.

| 물어볼 것 | 지금 |
|---|---|
| 결제가 안 돼요 | 문서 없음 |
| 환불 / 구독 해지 | 문서 없음 |
| **회원 탈퇴** | **기능 자체가 없다** |
| 「크레딧이 부족합니다」가 떴어요 | 문서 없음 |
| 이미지가 실패했는데 크레딧은 나갔나요 | 문서 없음 |

**회원 탈퇴는 특히 짚어야 한다.** `apps/web/app/settings/` 에는 프로필·로그인·
사용 기록 카드뿐이고, 탈퇴 길이 없다. 관리자가 지우는 길
(`adminDeleteMember`)만 있다. **봇이 답할 수 없는 게 아니라 답할 것이 없다.**

### 2.2 문의 채널

없다. 봇이 모를 때 넘길 곳이 없다.

### 2.3 봇용 사용량 칸

`GenerationOperation` 에 `cs_ask` 같은 값이 없다. **이 목록은 SQL 과 짝이다** —
`generation_events_operation_check` 제약과 `reserve_generation` 이 같은 목록을
보고, `lib/teams/__tests__/reserve-operation-whitelist.test.ts` 가 둘을 맞춰
놓는다. **칸을 하나 더하려면 마이그레이션이 필요하다.**

---

## 3. 가장 조심할 것 — 설명서의 숫자는 본문에 없다

`guide/credits/page.tsx` 의 주석이 사연을 적어 뒀다(2026-09-21).

> 전에는 이 표가 가중치를 들고 있었다(표준형 4장). 그런데 차감은 2026-09-08
> 부터 원가에서 나온다 (…) **값 안내가 틀리면 없는 것만 못하다.**
> 그래서 숫자를 안 적는다. **쓰는 그 함수로 그 자리에서 셈한다.**

즉 소스에는 이렇게 적혀 있다.

```tsx
function 한장당(model) { return creditUnits(unitPrice(model, "t2i", 기준크기)); }
```

**소스를 긁어 RAG 에 넣으면 봇은 「5장」이 아니라 `creditUnits(...)` 를 읽는다.**
그리고 그것을 사용자에게 옮긴다.

### 넣을 글을 어떻게 만드나 — 세 길

| | 방법 | 좋은 점 | 나쁜 점 |
|---|---|---|---|
| A | 소스(TSX)를 긁는다 | 쉽다 | **숫자가 안 들어간다.** 컴포넌트 이름·클래스가 지식으로 들어간다 |
| B | 그려진 HTML 을 긁는다 | 숫자가 맞는다 | `/guide/credits` 와 `/guide` 는 **세션을 읽는다**(`getMembership`). 누구로 그리느냐에 따라 글이 달라진다 |
| C | **지식을 따로 쓴다**(마크다운) | 봇이 읽기 좋은 글, 결제·탈퇴도 같은 자리에 | 설명서와 **두 벌**이 된다. 한쪽만 고치는 날이 온다 |

**권하는 것은 B + C 다.**

- **B** — 세션을 안 읽는 아홉 페이지는 그려진 글을 긁는다. 숫자가 맞는다.
  세션을 읽는 둘(`/guide`·`/guide/credits`)은 **손님으로 그린다** —
  설명서는 원래 로그인 없이 열리므로 그 글이 정본이다.
- **C** — 없는 영역(결제·환불·탈퇴·오류)은 새로 쓴다. 그 글은
  **사람이 읽어도 값이 있다** — `/guide` 에 페이지로도 올린다.

**두 벌을 안 만드는 방법**: C 로 쓴 마크다운을 `/guide` 페이지가 **읽어서 그린다.**
그러면 정본이 하나다. 이것이 설계의 핵심 결정이다.

---

## 4. 범위 — 안내인가 처리인가

| | 안내만 | 처리까지 |
|---|---|---|
| 「탈퇴하고 싶어요」 | 탈퇴하는 길을 알려 준다 | 계정을 지운다 |
| 「크레딧이 안 들어왔어요」 | 확인할 곳을 알려 준다 | 크레딧을 넣어 준다 |
| 「환불해 주세요」 | 절차를 알려 주고 문의로 넘긴다 | 환불한다 |
| 되돌리기 | 필요 없다 | 필요하다 |
| LLM 이 틀렸을 때 | 사용자가 한 번 더 묻는다 | **돈·계정이 사라진다** |

**1차 범위는 안내다.** 처리는 이 문서의 4단계로 미룬다.

**읽기는 안전하다.** 「제 크레딧이 얼마예요?」에 실제 잔액을 보여 주는 것은
위험이 낮고 문의의 상당수를 덮는다. 2차에 넣는다.

---

## 5. 구조

```
   사용자 질문
        │
        ▼
  ┌───────────────┐   ① 물어볼 만한 것인가 (형식·길이·빈도)
  │  문지기        │      로그인 여부에 따라 무엇을 읽을 수 있나
  └───────────────┘
        │
        ▼
  ┌───────────────┐   ② 무엇에 대한 물음인가
  │  갈래 가르기   │      안내 / 내 계정 / 오류 / 사람에게
  └───────────────┘
        │
        ├──────────────► 안내:  RAG 검색 (kind='guide')
        │                        └ 유사도 하한 미달 → 모른다
        │
        ├──────────────► 내 계정: 잔액·플랜·최근 실패 (2차)
        │
        └──────────────► 사람에게: 문의 남기기 (메일)
        │
        ▼
  ┌───────────────┐   ③ 찾은 조각만 근거로 답을 쓴다
  │  답 쓰기       │      근거가 없으면 「모릅니다」
  └───────────────┘
        │
        ▼
   답 + 출처 + 문의 단추
```

### 5.1 새 지식 갈래

`KnowledgeKind` 에 `"guide"` 를 더한다. 지금은 `"sales" | "redesign"` 둘이고,
주석이 갈래를 나눈 이유를 적어 뒀다 —

> 성격이 전혀 달라서 한 통에서 검색하면 판매 원칙을 찾을 때 남의 페이지 문구가
> 딸려 나온다.

**CS 지식도 같은 이유로 제 통을 쓴다.** 남의 상세페이지 전사가 CS 답변의 근거로
딸려 나오면 안 된다.

`normalizeKnowledgeKind` 의 기본값이 `redesign` 이므로, 기존 행은 그대로 남고
기존 호출부는 안 바뀐다.

### 5.2 근거를 보여 준다

답 아래에 **어느 문서 어느 대목**인지 적고 그 페이지로 가는 링크를 단다.

이유 둘:
- **틀렸을 때 사용자가 바로 안다.** 근거를 숨기면 틀린 답과 맞는 답이 똑같이
  보인다
- 사용자가 더 읽고 싶을 때 갈 곳이 생긴다

이 저장소가 이미 같은 결정을 여러 번 했다 — 심사 자국(`reviewStampOf`),
근거 기록(`evidence`), 참조 예산 고지.

### 5.3 모를 때

**세 가지를 지킨다.**

1. **유사도 하한 미달이면 검색 결과를 아예 안 쓴다**(`filterByRelevance` 가
   이미 한다)
2. 근거가 없으면 **지어내지 않고** 「이건 제가 모릅니다」라고 말한다
3. 그 자리에서 **문의 남기기**로 이어 준다

> 모를 때 아는 척하면, 사용자는 다음에 맞는 답이 왔을 때도 안 믿는다.

### 5.4 비용과 한도

- `GenerationOperation` 에 `cs_ask` 를 더한다 → **마이그레이션 필요**
  (`generation_events_operation_check` 제약 + `reserve_generation` 목록)
- 크레딧은 **0 장**이다. 그림을 안 만든다
- 그래도 **글 모델 값과 임베딩 값은 나간다.** `withLlmMeter` 로 잡아
  장부에 싣는다 — 이 저장소가 「값이 나가는데 장부에 0 원」인 자리를
  세 번 고쳤다(`reference_analyze`·`redesign_transcribe`·`pdp_analyze`)
- 시간당 한도를 `HOURLY_LIMITS` 에 더한다. 물음은 값싸니 넉넉히(예: 60)

### 5.5 로그인 안 한 사람

설명서가 공개이므로 **봇도 공개로 둘 수 있다.** 다만 다르다.

| | 손님 | 회원 |
|---|---|---|
| 설명서 물음 | 된다 | 된다 |
| 내 계정 물음 | 안 된다(로그인 안내) | 된다 |
| 한도 | IP 기준, 더 좁게 | 계정 기준 |
| 문의 남기기 | 메일 주소를 받는다 | 계정 메일을 쓴다 |

**손님에게 여는 것은 값이 나가는 문을 여는 것이다.** 한도와 봇 방지
(`NEXT_PUBLIC_TURNSTILE_SITE_KEY` 가 이미 있다)를 함께 건다.

---

## 6. 단계

### 1단계 — 안내 봇 (근거가 있는 것만)

| 할 일 | 파일 |
|---|---|
| `KnowledgeKind` 에 `guide` 더하기 | `packages/redesign-core/src/rag.ts` |
| 설명서를 글로 뽑는 스크립트 | `scripts/index-guide.mjs` (새로) |
| 갈래 가르기·답 쓰기 판단 | `apps/web/lib/cs/ask.ts` (새로, 순수) |
| 부르는 자리 | `apps/web/app/api/cs/ask/route.ts` (새로) |
| 화면 | `apps/web/app/_components/cs-bot.tsx` (새로) |
| 사용량 칸 | `supabase/migrations/…_cs_operation.sql` (새로) |

**이 단계에서 지킬 것**
- 근거 없는 답을 안 한다
- 출처를 보여 준다
- 모르면 문의로 넘긴다
- 값이 장부에 실린다

### 2단계 — 없는 문서 쓰기

**봇보다 이게 먼저 값을 낸다.** 사람이 읽어도 도움이 되기 때문이다.

- 결제 · 환불 · 구독 해지
- **회원 탈퇴** — 문서보다 **기능이 먼저다**(§2.1)
- 오류 대응: 크레딧 부족 · 생성 실패 · 한도 초과 · 업로드 거절
- 「실패했는데 크레딧은?」 — 실제 규칙을 적는다(성공한 것만 차감)

마크다운으로 쓰고 `/guide` 가 그것을 읽어 그린다. **정본은 하나다.**

### 3단계 — 내 계정 읽기

잔액 · 플랜 · 이번 달 사용량 · 최근 실패한 작업과 그 까닭.

「왜 안 만들어졌어요?」에 **실제 이유**를 답할 수 있게 된다. 지금은
`generation_events` 에 `error_code` 가 남아 있어 읽을 수 있다.

### 4단계 — 처리 (별도 판단)

정말 필요하다고 확인된 것만, **되돌릴 수 있는 것만.** 이 문서는 여기까지
설계하지 않는다.

---

## 7. 재는 방법

이 저장소의 규칙대로 **값으로 잰다.**

| 무엇을 | 어떻게 |
|---|---|
| 갈래 가르기 | 순수 모듈. 물음 표본으로 표 시험 |
| 모를 때 안 지어내는가 | 검색 결과를 비워 두고 답을 본다. 「모릅니다」가 나와야 한다 |
| 유사도 하한 | 무관한 물음에 엉뚱한 조각이 안 붙는지 |
| 출처 표시 | 답에 실제로 링크가 붙는지(**띄워서**) |
| 장부 | `cs_ask` 가 `generation_events` 에 실리는지 |
| 문 | 손님이 내 계정을 못 읽는지 |
| 설명서 색인 | **숫자가 들어갔는지** — 「5장」이 있고 `creditUnits(` 는 없어야 한다 |

마지막 줄이 §3 의 함정을 막는 그물이다.

---

## 8. 안 하기로 한 것

- **처리(탈퇴·환불·크레딧 지급)를 1차에 넣지 않는다.** 되돌리기가 없는 일을
  LLM 판단에 맡기지 않는다
- **설명서와 CS 지식을 두 벌로 두지 않는다.** 한쪽만 고치는 날이 온다
- **모르는 것을 그럴듯하게 답하지 않는다.** 틀린 CS 답변은 없는 것만 못하다
- **사람에게 넘기는 길을 없애지 않는다.** 「모든 CS 를 AI 가」는
  「AI 만」이 아니다

---

## 9. 먼저 정해야 할 것

1. **회원 탈퇴 기능을 만들 것인가.** 지금 없다. 봇 이전의 문제다
2. **문의를 어디로 넘길까.** 메일(SMTP 있음) / 관리자 화면에 쌓기 / 둘 다
3. **손님에게도 열까.** 가입 전 문의가 많을 텐데, 값이 나가는 문이다
4. **어디에 둘까.** 우하단 떠 있는 단추 / `/guide` 안 / 별도 페이지
