/**
 * **지어내면 안 되는 것.** 두 도구가 함께 쓰는 한 벌(F-7-1).
 *
 * ── 왜 여기에 두나 ──────────────────────────────────────────
 *
 * 같은 말을 네 곳이 조금씩 다르게 적고 있었다.
 *
 *   상세페이지 · 제품 읽기   효능 · 수치 · 인증 · 후기 를 지어내지 않는다
 *   리디자인 · 분석          근거 없는 수치 · 효과 · 리뷰 · 인증
 *   리디자인 · 섹션          근거 없는 수치 · 리뷰 · 인증 · 효과
 *   리디자인 · 수정          근거 없는 효능 · 리뷰 · 인증 · 수치
 *
 * 「효과」와 「효능」, 「리뷰」와 「후기」는 사람에게는 같은 말이지만 **계약으로
 * 보면 네 벌**이다. 한 곳을 고쳐도 나머지 셋은 그대로 남고, 어느 것이 맞는지
 * 아무도 모른다.
 *
 * 설계 §14.6(F-7-1): 「별도 코어의 참조·검수·예산·**근거 규칙 불일치** |
 * **공통 실행/검수 계약 공유**, 도메인 전체 병합은 하지 않음」.
 *
 * **도메인을 합치는 것이 아니다.** 상세페이지는 상세페이지대로, 리디자인은
 * 리디자인대로 제 프롬프트를 쓴다. 지어내면 안 되는 것의 목록만 한 벌이다.
 *
 * ── 왜 법이 아니라 목록인가 ────────────────────────────────
 *
 * 표시광고법은 「근거 없는 효능·수치」를 광고에서 금지한다. 그런데 모델에게
 * 법 조문을 읽히는 것보다 **무엇을 지어내지 말라는지 낱말로 적는 쪽**이
 * 실제로 따라온다 — 이 저장소가 색 지시에서 이미 같은 것을 실측했다
 * (나열보다 쓰임새를 문장으로 적어야 반영된다, 2026-07-30).
 */

/**
 * 지어내면 안 되는 것들.
 *
 * **낱말 하나가 계약 한 줄이다.** 여기에 더하면 양쪽 프롬프트가 함께 따라와야
 * 하고, 안 따라오면 계약 시험이 빨개진다
 * (`apps/web/lib/__tests__/grounding-contract.test.ts`).
 */
export const NEVER_FABRICATE = ["수치", "효능", "인증", "후기"] as const;

/**
 * 프롬프트에 **글자 그대로** 싣는 한 문장.
 *
 * 목록만 맞추면 「각자 제 말로 적되 낱말만 같게」가 된다 — 그러면 계약을 고쳐도
 * 각 코어의 문장은 안 따라온다. 한 문장을 그대로 실으면 고칠 곳이 한 곳이다.
 *
 * 「효과」·「리뷰」를 함께 적는 이유는 두 도구가 쓰던 말을 잃지 않기 위해서다.
 * 모델이 좁게 읽는 것을 막는다.
 *
 * ── 여기 없는 것 ────────────────────────────────────────────
 *
 * 처음 판에는 「확인되지 않은 것은 아예 쓰지 않고」가 붙어 있었다. 리뷰가
 * 잡았다(2026-09-21) — 상세페이지에는 사용자가 고르는 **갭 정책**이 있고,
 * 그중 `sample` 은 「구체적인 값이 필요하면 현실적인 예시를 채운다」이다
 * (`pdp.product-reading.ts` 의 `photoGapPolicyRules`).
 *
 * 두 문장이 **같은 프롬프트 안에 있고** 이 계약이 뒤에 온다. 이 저장소는 「뒤가
 * 이긴다」를 이미 실측해 뒀으므로, **사용자가 고른 설정이 조용히 무력화된다.**
 * 그 판단은 `omit`·`sample`·`ask` 가 이미 하고 있다 — 계약은 **지어내면 안
 * 되는 목록**과 **위험 표현 완화**까지만 말한다.
 */
export const GROUNDING_RULE =
  "근거 없는 수치·효능·인증·후기(효과·리뷰 표현 포함)를 지어내지 않는다. 위험한 표현은 안전하게 완화한다.";
