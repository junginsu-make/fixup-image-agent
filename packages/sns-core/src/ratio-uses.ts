/**
 * **어떤 용도에 어떤 비율을 쓰나** — 비율 버튼 아래의 길잡이 한 줄.
 *
 * 유튜브 썸네일을 찾는 사람이 목록에서 헤맸다(2026-09-16 사용자 보고).
 * 1280×720 = 정확히 16:9 라 **이미 만들 수 있는데**, 버튼에 「가로 배너 16:9」라고만
 * 적혀 있어 그게 그거인 줄 모른다.
 *
 * **새 비율 항목을 만들지 않는다.** 같은 픽셀을 다른 이름으로 두면 목록에 같은
 * 것이 둘 생기고, 사용자는 뭐가 다른지 묻게 된다 — 그리고 `POSTER_RATIOS` 는
 * 값 계산·모델 고르기의 열쇠라 늘리면 그쪽까지 흔들린다.
 *
 * 대신 **버튼은 그대로 두고 아래에 길잡이**를 단다. 목록이 아니라 길잡이이므로
 * 짧게 유지한다 — 길면 아무도 안 읽는다.
 */

export interface RatioUse {
  /** 사람이 찾는 말. 「유튜브 썸네일」처럼 목적지 이름으로 적는다. */
  label: string;
  /** 그때 누를 버튼. `POSTER_RATIOS` 에 실제로 있어야 한다. */
  ratioId: string;
}

export const RATIO_USES: RatioUse[] = [
  { label: "유튜브 썸네일 · 가로 배너", ratioId: "16:9" },
  { label: "인스타그램 피드", ratioId: "4:5" },
  { label: "스토리 · 릴스 · 쇼츠", ratioId: "9:16" },
];

/*
 * **인쇄물은 여기 없다.** 버튼이 이미 「A4 인쇄용」이라고 말하고 있어 길잡이가
 * 보탤 것이 없다. 게다가 그 id 는 `a4-print` 라 화면에 그대로 뜨면 내부 이름을
 * 사용자에게 보이는 꼴이 된다(2026-09-16 화면 실측).
 *
 * 남긴 셋은 **버튼 이름과 다른 말로 찾는 것들**이다 — 특히 유튜브는 버튼 어디에도
 * 없어서 못 찾았다.
 */
