/**
 * **「원하는 톤」의 값 목록.**
 *
 * ── 무엇이 문제였나 (D-8) ────────────────────────────────────
 *
 * 화면은 칩 일곱 개다. 사용자가 고를 수 있는 것은 그 일곱뿐인데, 서버 스키마는
 * `z.string()` 이었다. 그리고 그 값은 **이미지 프롬프트에 그대로 실린다** —
 * `pdp.image-prompt.ts` 의 `Overall tone: ${options.desiredTone}.`
 *
 * 즉 **화면을 거치지 않은 요청이 프롬프트에 아무 문장이나 심을 수 있었다.**
 * 길이 제한도 없었다.
 *
 * 설계 §14.4(D-8): 「country/age **등** enum 경계 부족 | 수정」. 그 두 값은
 * 이미 `z.enum` 으로 묶여 있고, 남아 있던 것이 이 칸이다.
 *
 * ── 왜 코어에 두나 ───────────────────────────────────────────
 *
 * 화면이 칩을 만들고 서버가 검사한다. 목록이 두 벌이면 **화면이 보여 주는
 * 값을 서버가 거절하는 날**이 온다 — 그때 사용자가 보는 것은 「요청이 올바르지
 * 않습니다」 한 줄이다.
 */

/**
 * 전선에 실리는 값. **「AI 자동 추천」은 여기 없다** — 그것은 「안 고름」이고
 * 빈 값으로 간다.
 */
export const IMAGE_TONES = [
  "프리미엄",
  "모던",
  "테크",
  "미니멀",
  "팝아트",
  "인스타감성",
  "레트로",
] as const;

export type ImageTone = (typeof IMAGE_TONES)[number];

/** 안 고른 상태의 화면 이름. 값은 빈 글자다. */
export const TONE_AUTO_LABEL = "AI 자동 추천";
