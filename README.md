# MCS — Marketing Content Studio

소재를 모으고, 그것으로 콘텐츠를 만들고, 만든 것을 다시 쓰는 곳.

카드뉴스 · 이미지(광고 소재 · 포스터) · 상세페이지 · 리디자인 · 캐릭터를 한 곳에서 만듭니다.
`detail-page-studio` 를 씨앗으로 복제했으나 이후로는 별개 제품입니다. 원본을 참조하지 않습니다.

```
수집 리스트 ─→ 수집함 ─┐
                       ├─→ 라이브러리 ─→ 만들기 ─→ 작업물 ─┐
사용자가 올린 참고 이미지 ┘         ↑                        │
                                   └────────────────────────┘
                                     만든 것을 다시 재료로
```

**한 바퀴가 닫혀 있는 것이 이 시스템의 뼈대입니다.** 등록한 모든 것이 라이브러리에 쌓이고,
어느 도구에서든 그것을 불러 쓰고, 만든 결과물이 다시 라이브러리로 들어갑니다.

## 도구

| 도구 | 하는 일 |
|---|---|
| **카드뉴스** (`/sns`) | 수집한 글이나 직접 쓴 글을 여러 장의 카드로. 원고를 사람이 확인한 뒤 그림을 만들고, 만든 그림은 글자가 원고대로 들어갔는지 검수합니다 |
| **이미지 만들기** (`/poster`) | 광고 소재 · 포스터 · 일반 이미지 한 장. 따라 만들 이미지를 고르고 한 줄 적으면 나머지 칸은 AI 가 초안으로 채웁니다 |
| **상세페이지 만들기** (`/create`) | 상품 사진 한 장 **또는 글만으로** 상세페이지를 새로 |
| **리디자인** (`/redesign`) | 기존 상세페이지(이미지 · PDF)를 분석해 전환율 중심으로 개선 |
| **캐릭터** (`/characters`) | 인물을 정면 · 좌 · 우 · 후면으로 고정해 두고 재사용 |
| **수집함 · 수집 리스트** (`/inbox`, `/sources`) | 유튜브 · RSS · 네이버 뉴스 · 커뮤니티에서 소재를 자동으로 모읍니다 |
| **라이브러리** (`/library`) | 참고 이미지 · 묶음 세트 · 수집한 글 · 작업물. 로그인하면 여기가 먼저 열립니다 |

참고 이미지와 작업물을 나누는 기준은 하나입니다 — **참고 이미지는 사용자가 첨부한 것, 작업물은
이 시스템이 만든 것.**

## 품질 장치

### 원고와 그림을 나눠서 만든다

카드뉴스는 기획 → 원고 → 그림 순서로 갑니다. **원고 단계에서 사람이 글자를 직접 고칩니다.**
고친 글자가 그대로 그림에 들어갑니다. 그림을 만든 뒤에 글자를 고치려면 다시 만들어야 하는데,
그건 돈이 드는 일입니다.

만든 그림은 다른 호출로 검수합니다 — 원고에 없는 글자가 들어갔는지, 글자가 바뀌었는지.
검수 결과는 한 줄로 먼저 보여주고 자세한 것은 접어 둡니다. 스키마의 영문 값(`extraCopy`,
`uncertain`)은 우리말로 바꿔서 보여줍니다.

### 구성안 심사 (상세페이지)

구성안을 만든 호출과 **다른 호출**로 심사합니다. 같은 호출 안에서 매기는 점수는 방금 쓴 글을
스스로 칭찬하는 것에 가깝습니다.

심사자에게는 구성안과 판매 원칙만 줍니다. 브리프 원문은 주지 않습니다 — 주면 "이 브리프로는
이 정도면 잘 쓴 것"이라는 변호를 합니다. 사는 사람은 브리프를 못 봅니다.

항목은 여섯입니다: **대상 · 문제 · 차별점 · 반론 · 흐름 · 행동 유도**. `fail` 이 하나라도 있으면
지적사항을 담아 다시 만듭니다(최대 2회). 끝까지 남은 지적은 **숨기지 않고 화면에 띄웁니다.**

판매 원칙은 `packages/pdp-core/src/pdp.sales-principles.ts` 가 단일 출처입니다.

### 참고 이미지를 어떻게 쓰는지 말로 정한다

레퍼런스 이미지는 효과가 세서 어떻게 쓸지 못박아 둡니다. 역할 어휘는 네 가지로 통일돼 있습니다
(`packages/shared/src/attachment-role.ts`).

```
따라 만들기          그 결을 따라 새로 그린다
제품 그대로 지키기    그 물건은 바꾸지 않는다
인물 그대로 지키기    그 사람은 바꾸지 않는다
원본 그대로 넣기      생성하지 않고 그 그림을 그 자리에 넣는다
```

상세페이지의 스타일 레퍼런스는 **페이지당 한 장**만 씁니다. 섹션마다 다르면 통일이 깨집니다.
어울리는 것이 없으면 아무것도 쓰지 않습니다. 고르는 것은 유사도 검색이 아니라 LLM 입니다 —
질의는 상품 얘기인데 레퍼런스 서술은 디자인 얘기라 임베딩으로는 변별되지 않습니다(측정 근거:
`docs/superpowers/specs/2026-07-27-style-reference-rag-design.md`).

### 이미지 모델

생성은 fal.ai 를 경유합니다. 원가가 4.6배까지 벌어져 크레딧 가중치를 둡니다.

| 모델 | 가중치 | 특징 |
|---|---|---|
| GPT Image 2 (기본) | 4 | 글자가 가장 정확. 명조 계열도 표현 |
| Nano Banana Pro | 3 | 빠름. 글자는 고딕 계열 |
| Nano Banana | 1 | 가장 저렴. 글자가 적은 장면에 |

상세페이지 섹션은 한 장씩이 아니라 **묶음으로 한 번에** 만듭니다. 순차 호출은 장마다 크레딧을
예약해 동시 실행 제한에 스스로 걸렸습니다.

### 만드는 도중에 화면을 옮겨도 된다

생성은 브라우저가 이끕니다. fal 에 요청을 보내 두면 fal 은 알아서 그리지만 **결과를 받아 오는
것은 화면**입니다. 그래서 예전에는 화면을 옮기면 받아 오는 일이 멈췄습니다.

지금은 목록을 셸(사이드바)이 들고 있습니다. 셸은 화면을 옮겨도 다시 만들어지지 않으므로 계속
받아 옵니다. 사이드바 아래에 무엇이 돌고 있는지, 얼마나 됐는지 보이고 중지할 수 있습니다.

**중지는 되돌리기가 아닙니다.** fal 에 이미 보낸 요청은 취소하지 못합니다. 멈추는 것은 받아
오는 일뿐이고 보낸 요청의 비용은 그대로 나갑니다. 그 자리에 그렇게 적어 뒀습니다.

## 구조 (pnpm 모노레포)

```
apps/web/                Next.js 프론트엔드 + 얇은 API 어댑터 (단일 배포 대상)
  app/                   /library(로그인 후 첫 화면) · /sns · /poster · /create
                         /redesign · /characters · /inbox · /sources · /settings · /admin
  app/page.tsx           공개 랜딩(/). 규칙은 docs/landing.md
  app/_landing/          랜딩 섹션들. 이 페이지 밖에서는 쓰지 않는다
  app/_components/       셸에 붙는 것들 — 만드는 중 목록 · 이미지 뷰어 · 라이브러리 선택창
  lib/membership/        회원 권한 · 월 이미지 크레딧 · API 게이트
  lib/supabase/          브라우저 / SSR / 서버 전용 Supabase 클라이언트
  lib/sns/               카드뉴스 흐름 — 큐 · 런타임 · 제공자
  lib/poster/            이미지 만들기 저장소 (로컬 파일 · Supabase 양쪽)
  lib/running-jobs.ts    만드는 중 목록 (브라우저에 저장)
apps/worker/             수집 워커. 5분마다 돌 차례가 된 소스를 긁는다
packages/sns-core/       카드뉴스 로직 — 기획 · 원고 · 게시글 문구 · 프롬프트 · 검수
packages/poster-core/    이미지 만들기 로직 — 슬롯 · 문법 · 가격 · 검수
packages/ingest-core/    수집 어댑터 — 유튜브 · RSS · 네이버 뉴스 · 커뮤니티
packages/pdp-core/       상세페이지 백엔드 (텍스트 진입 · 심사 · 이미지 제공자)
packages/redesign-core/  리디자인 백엔드 + RAG 지식 저장소
packages/ui/             공통 디자인 시스템 (Tailwind v4 + shadcn, 다크모드)
packages/shared/         교차 타입 — 역할 어휘 · 제공자 대체 규칙
```

설계 · 구현 문서: `docs/superpowers/specs/`, `docs/superpowers/plans/`
공개 랜딩페이지 규칙: `docs/landing.md`

## 데이터

**하나의 Supabase 프로젝트**에 전부 들어갑니다. 회원 · 수집한 글 · 참고 이미지 · 만든 결과물.
상세페이지 서비스와 같은 프로젝트를 쓰므로 **계정이 공유됩니다.**

파일은 비공개 버킷에 두고 짧은 수명의 서명 URL 로만 엽니다. 경로 첫 칸이 소유자이고,
버킷 정책이 그 칸으로 남의 것을 막습니다.

```
{user_id}/references/{id}.{ext}       참고 이미지
{user_id}/sns/{작업}/{카드}.png        카드뉴스 결과
{user_id}/poster/{작업}/{변형}.png     이미지 만들기 결과
```

리디자인이 참고하는 디자인 지식만 별도 DB(Neon pgvector)에 있습니다. 회원 데이터나 생성
결과물은 들어가지 않습니다.

로컬에서는 `LOCAL_STORE=1` 로 파일 저장소를 씁니다. `NODE_ENV=production` 에서는 이 값들이
무시되므로 운영에서 실수로 켜지지 않습니다.

## 개발

```bash
corepack enable pnpm      # 최초 1회
pnpm install
pnpm dev                  # http://localhost:3000
```

기타: `pnpm -r typecheck`, `pnpm -r test`, `pnpm build`

로컬은 `apps/web/.env.local` 에서 `LOCAL_STORE=1` · `LOCAL_AUTH_BYPASS=1` 로 로그인 없이
파일 저장소를 씁니다. Supabase 값을 채우면 운영 DB 를 건드리므로 비워 둡니다.

## 배포

**운영은 EC2 입니다.** 저장소에 연결된 Vercel 프로젝트는 구버전이며 운영이 아닙니다.
PR 체크에 Vercel 이 뜨더라도 배포 판단 근거로 삼지 마십시오.

```
브라우저 ─→ Caddy(80) ─→ Next.js standalone(127.0.0.1:3000)
                          + 수집 워커 (systemd)
                        ─→ Supabase (DB · 파일 · 로그인)
```

절차와 확인 목록은 `docs/DEPLOY.md` 에 있습니다. 요약하면,

```bash
# master 에 push 하면 GitHub Actions 가 꾸러미를 만든다
gh run download <run-id> --dir ./release
scp release/.../fixup-image-agent-<sha12>.tar.gz ubuntu@<host>:/tmp/
ssh ubuntu@<host> 'sudo bash deploy/ec2/deploy-release.sh /tmp/fixup-image-agent-<sha12>.tar.gz'
```

배포 스크립트가 기동을 확인하고, 실패하면 **직전 릴리스로 자동 롤백**합니다. 웹이 건강한 것을
본 뒤에야 워커를 넘깁니다 — 웹이 카나리아입니다. 수동 롤백은 `deploy/ec2/rollback-release.sh`.

호스트에 개인 배포(`detail-page-studio`)가 있으면 설치 스크립트가 스스로 멈춥니다. 둘 다 3000
포트를 쓰고 설치가 systemd 와 Caddy 설정을 건드리기 때문입니다.

### 꾸러미가 이식 가능해야 하는 이유

`dist/ec2` 에는 standalone 서버, workspace 런타임 의존성, `public`, `.next/static`, 그리고
묶어 둔 수집 워커만 모이며 로컬 `.env*` 는 제거됩니다.

여기서 두 번 데었습니다. 둘 다 **배포해 봐야 아는 실패**였고, 지금은 둘 다 빌드가 막습니다.

**하나. 심볼릭 링크가 빌드 머신을 가리켰다.** pnpm 이 만든 링크가 절대 경로여서 꾸러미가 빌드한
기계에서만 돌았습니다. 2026-07-28 운영 배포에서 `Cannot find module 'next'` 로 죽고 롤백된 뒤에야
드러났습니다. 지금은 상대 경로로 바꿉니다. 링크 구조 자체는 유지해야 합니다 — pnpm 은 그 구조로
모듈을 해석해서, 실제 파일로 풀어버리면 `next` 는 찾아도 그 옆의 `styled-jsx` 를 못 찾습니다.

**둘. sharp 의 네이티브 라이브러리가 자리를 잃었다.** `.node` 바인딩은 `libvips-cpp.so` 를 OS
수준에서 엽니다. 자바스크립트 require 가 아니라 Next 의 추적에 안 잡히고, 그래서 그 `.so` 가
빠진 채 배포됐습니다. 이미지를 다루는 API 가 죽고 Next 가 HTML 오류 페이지를 돌려주니, 화면에는
`Unexpected token '<'` 만 떴습니다 — 원인과 증상이 멀어 찾는 데 오래 걸렸습니다.

두 번째는 한 번 더 틀렸습니다. 처음 고쳤을 때 "꾸러미 어딘가에 있는가" 로 검사했는데 그건
통과했습니다. 파일은 있었으니까요. 정작 필요한 것은 **바인딩이 찾는 그 자리에 있는가** 였습니다.
지금은 그 자리를 봅니다.

### 운영 환경변수

호스트의 `/etc/fixup-image-agent/app.env`(root 소유, `640`)에서만 읽습니다. systemd
`EnvironmentFile` 로 주입되며 꾸러미에는 들어가지 않습니다.

이름은 전부 코드가 실제로 읽는 것만 적혀 있습니다 — 안 읽는 이름을 적어 두면 채워 놓고도 안 되는
이유를 찾느라 시간을 씁니다. 템플릿은 `deploy/ec2/app.env.example`.

`FAL_KEY` 가 없으면 이미지가 한 장도 안 만들어집니다. `APIFY_TOKEN` 이 없으면 유튜브 수집이
실패합니다 — EC2 는 유튜브 자막 API 에서 IP 로 막히기 때문에 그 우회가 필수입니다.

## 아직 안 되는 것

- **공식 AI 블로그 수집** — 그 어댑터만 playwright(브라우저)를 씁니다. 1GB 서버에서 Chromium 을
  띄우면 웹까지 같이 죽어서 꾸러미에서 뺐습니다. 켜면 그 소스만 실패하고 나머지 수집은 돕니다.
- **HTTPS** — 도메인 없이 IP 로만 열어 두어 평문입니다. 비밀번호는 브라우저가 Supabase 로 직접
  보내므로(그 구간은 HTTPS) 새지 않지만, 로그인 뒤 세션 토큰은 평문으로 오갑니다. 내부용이라는
  전제에서만 괜찮습니다.
- **마이그레이션 기록** — 표는 대시보드 SQL 편집기와 MCP 로 넣었습니다. `supabase db push` 는
  기록이 어긋나 쓰지 않습니다.

## 참고

소스 코드의 모델 ID(`gpt-image-2`, `nano-banana-pro`, `gemini-3.1-pro-preview` 등)는 실제로
접근 가능한 것들입니다. 동작은 키와 모델 접근 권한에 의존합니다.

구현 중 측정으로 뒤집힌 설계는 문서에 이유와 함께 남겨 뒀습니다 — 특히 **작고 전부 필요한
지식은 검색(RAG)하지 말고 프롬프트에 통째로 넣어야 한다**는 것이 두 번 확인됐습니다.
